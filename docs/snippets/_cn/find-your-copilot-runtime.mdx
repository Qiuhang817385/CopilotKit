### 找到您的 CopilotRuntime

本节的起点是您在快速入门期间设置的 `CopilotRuntime`（CopilotKit 后端端点）。
如需复习，请参阅[自托管](/guides/self-hosting)（或者，重新访问[快速入门](/quickstart)）。

**首先，在您的代码中找到 `CopilotRuntime` 实例。** 您只需在代码库中搜索 `CopilotRuntime` 即可。

如果您按照快速入门操作，它将位于您设置 `/api/copilotkit` 端点的位置。
