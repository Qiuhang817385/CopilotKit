---
title: "GroqAdapter"
description: "适用于 Groq 的 Copilot Runtime 适配器。"
---

{
 /*
  * 注意！请勿修改此文件！
  * 此页面是自动生成的。如果你想对此页面进行任何更改，必须在以下位置进行：
  * packages/v1/runtime/src/service-adapters/groq/groq-adapter.ts
  */
}
适用于 Groq 的 Copilot Runtime 适配器。
 
## 示例
 
```ts
import { CopilotRuntime, GroqAdapter } from "@copilotkit/runtime";
import { Groq } from "groq-sdk";
 
const groq = new Groq({ apiKey: process.env["GROQ_API_KEY"] });
 
const copilotKit = new CopilotRuntime();
 
return new GroqAdapter({ groq, model: "<model-name>" });
```

## 构造函数参数

<PropertyReference name="groq" type="Groq"  > 
可选的 Groq 实例。
</PropertyReference>

<PropertyReference name="model" type="string"  > 
要使用的模型。
</PropertyReference>

<PropertyReference name="disableParallelToolCalls" type="boolean"  default="false"> 
是否禁用并行工具调用。
  你可以禁用并行工具调用以强制模型按顺序执行工具调用。
  当你希望按特定顺序执行工具调用，以便一个工具调用引入的状态更改对下一个工具调用可见时，这很有用。（即新操作或可读项）
</PropertyReference>
