---
title: 工具渲染
icon: "lucide/Server"
description: 使用自定义 UI 组件渲染智能体的工具调用。
hideTOC: true
---
import { Accordions, Accordion } from "fumadocs-ui/components/accordion";
import { IframeSwitcher } from "@/components/content"
import { Tabs, Tab } from "fumadocs-ui/components/tabs"
import RunAndConnect from "@/snippets/integrations/langgraph/run-and-connect.mdx"
import DefaultToolRendering from "@/snippets/shared/guides/default-tool-rendering.mdx"

<IframeSwitcher
  id="backend-tools-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/backend_tool_rendering?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/backend_tool_rendering?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout>
  此示例演示了在 <a href="https://feature-viewer.copilotkit.ai/langgraph/feature/agentic_chat" target="_blank">CopilotKit 功能查看器</a> 中应用的[实现](#实现)部分。
</Callout>

## 这是什么？

工具是 LLM 调用预定义、通常是确定性函数的一种方式。CopilotKit 允许你在 UI 中将这些工具渲染为自定义组件，我们称之为**生成式 UI**。

## 什么时候使用？

在 UI 中渲染工具在你想向用户提供关于智能体正在做什么的反馈时很有用，特别是当你的智能体正在调用工具时。CopilotKit 允许你完全自定义这些工具在聊天中的渲染方式。

## 实现

<Steps>
  <Step>
    ### 运行并连接你的智能体
    <RunAndConnect components={props.components} />
  </Step>
<Step>
### 给你的智能体一个可调用的工具

<Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
    <Tab value="Python">
        ```python title="agent.py"
        from langchain_openai import ChatOpenAI
        from langchain.tools import tool
        # ...

        # [!code highlight:6]
        @tool
        def get_weather(location: str):
            """
            获取指定位置的天气。
            """
            return f"{location} 的天气是 70 度。"

        # ...

        async def chat_node(state: AgentState, config: RunnableConfig):
            model = ChatOpenAI(model="gpt-5.2")
            model_with_tools = model.bind_tools([get_weather]) # [!code highlight]

            response = await model_with_tools.ainvoke([
                SystemMessage(content=f"你是一个有用的助手。"),
                *state["messages"],
            ], config)

            # ...
        ```
    </Tab>
    <Tab value="TypeScript">
        ```typescript title="agent-js/src/agent.ts"
        import { ChatOpenAI } from "@langchain/openai";
        import { tool } from "@langchain/core/tools";
        import { z } from "zod";

        // [!code highlight:12]
        const get_weather = tool(
          (args) => {
            return `${args.location} 的天气是 70 度。`;
          },
          {
            name: "get_weather",
            description: "获取指定位置的天气。",
            schema: z.object({
              location: z.string().describe("要获取天气的位置"),
            }),
          }
        );

        async function chat_node(state: AgentState, config: RunnableConfig) {
          const model = new ChatOpenAI({ temperature: 0, model: "gpt-5.2" });
          const modelWithTools = model.bindTools([get_weather]); // [!code highlight]

          const response = await modelWithTools.invoke([
            new SystemMessage("你是一个有用的助手。"),
            ...state.messages,
          ], config);

          // ...
        }
        ```
    </Tab>
</Tabs>
</Step>
<Step>
### 在你的前端渲染工具调用
此时，你的智能体将能够调用 `get_weather` 工具。现在
我们只需要添加一个 `useRenderTool` 钩子来在 UI 中渲染工具调用。

<Callout type="info" title="重要">
  要在 UI 中渲染工具调用，名称必须与工具名称匹配。
</Callout>

```tsx title="app/page.tsx"
import { useRenderTool } from "@copilotkit/react-core/v2"; // [!code highlight]
import { z } from "zod";
// ...

const weatherParams = z.object({
  location: z.string().describe("要获取天气的位置"),
});

const YourMainContent = () => {
  // ...
  // [!code highlight:14]
  useRenderTool({
    name: "get_weather",
    parameters: weatherParams,
    render: ({ status, parameters }) => {
      return (
        <p className="text-gray-500 mt-2">
          {status !== "complete" && "正在调用天气 API..."}
          {status === "complete" && `已为 ${parameters.location} 调用天气 API。`}
        </p>
      );
    },
  });
  // ...
}
```

</Step>
<Step>
### 试一试！

试着让智能体获取某个位置的天气。你应该看到我们添加的自定义 UI 组件渲染工具调用并显示传递给工具的参数。

</Step>
</Steps>

## 默认工具渲染

<DefaultToolRendering components={props.components} />
