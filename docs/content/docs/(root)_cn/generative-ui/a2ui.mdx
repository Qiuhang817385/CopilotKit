---
title: "A2UI"
icon: "lucide/FileJson"
description: "使用 A2UI 规范渲染声明式 UI 组件。"
hideTOC: true
---
import { IntegrationGrid } from "@/components/content/integration-grid";

## 这是什么？

A2UI（Agent-to-UI）是生成式 UI 的声明式规范。它允许 Agent 使用基于 JSON 的 schema 渲染结构化 UI 组件，而无需为每个交互自定义 React 组件。

## 选择你的 AI 后端

<IntegrationGrid path="generative-ui/a2ui" />
