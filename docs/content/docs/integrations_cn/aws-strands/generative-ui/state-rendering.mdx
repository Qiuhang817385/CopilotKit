---
title: 状态渲染
icon: "lucide/Bot"
description: 使用自定义 UI 组件渲染 Agent 的状态。
---
import { Accordions, Accordion } from "fumadocs-ui/components/accordion";
import { IframeSwitcher } from "@/components/content"
import { Tabs, Tab } from "fumadocs-ui/components/tabs"
import RunAndConnect from "@/snippets/integrations/aws-strands/run-and-connect.mdx"

<IframeSwitcher
  id="agent-state-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/aws-strands/feature/agentic_generative_ui?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/aws-strands/feature/agentic_generative_ui?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

## 这是什么？

AWS Strands Agent 在处理任务时维护状态。CopilotKit 允许你使用自定义 UI 组件在应用中渲染此状态，我们称之为**Agent 生成式 UI**。

## 何时使用？

当你在 UI 中渲染 Agent 的状态时，可以为用户提供有关会话整体状态的反馈。一个很好的例子是用户和 Agent 一起解决问题的场景。Agent 可以在其状态中存储草稿，然后在 UI 中渲染。

## 实现

<Steps>
  <Step>
    ### 运行并连接你的 Agent
    <RunAndConnect components={props.components} />
  </Step>
  <Step>
    ### 设置带状态的 AWS Strands Agent

    配置你的 Strands Agent 以维护状态。以下是一个跟踪搜索的示例：

    ```python title="agent/main.py"
    import json
    from ag_ui_strands import StrandsAgent, StrandsAgentConfig, ToolBehavior, create_strands_app
    from strands import Agent, tool
    from strands.models.openai import OpenAIModel

    # 创建更新状态的工具
    @tool
    def add_search(query: str):
        """将搜索添加到 Agent 的搜索列表中。

        Args:
            query: 要添加的搜索查询

        Returns:
            成功状态和查询
        """
        return json.dumps({"success": True, "query": query})

    # 从工具参数中提取状态
    async def searches_state_from_args(context):
        try:
            tool_input = context.tool_input
            if isinstance(tool_input, str):
                tool_input = json.loads(tool_input)
            query = tool_input.get("query", "")
            return {"searches": [{"query": query, "done": True}]}
        except Exception:
            return None

    config = StrandsAgentConfig(
        tool_behaviors={
            "add_search": ToolBehavior(
                state_from_args=searches_state_from_args,
            ),
        },
    )

    model = OpenAIModel(
        client_args={"api_key": os.getenv("OPENAI_API_KEY", "")},
        model_id="gpt-4o",
    )

    strands_agent = Agent(
        model=model,
        system_prompt="你是一个 helpful 的助手，用于存储搜索。使用 add_search 工具来添加搜索。",
        tools=[add_search],
    )

    agui_agent = StrandsAgent(
        agent=strands_agent,
        name="searchAgent",
        description="一个 helpful 的搜索存储助手",
        config=config,
    )

    app = create_strands_app(agui_agent)
    ```

  </Step>
  <Step>
    ### 在聊天中渲染 Agent 状态

    现在我们可以利用带有 `render` 函数的 `useAgent` 在**聊天中**渲染我们 Agent 的状态。

    ```tsx title="app/page.tsx"
    // ...
    import { useAgent } from "@copilotkit/react-core/v2";
    // ...

    // 定义 Agent 的状态，应与你的 Strands Agent 的状态 schema 匹配
    type AgentState = {
      searches: {
        query: string;
        done: boolean;
      }[];
    };

    function YourMainContent() {
      // ...

      // [!code highlight:13]
      // 为简洁起见，省略了样式
      useAgent({
        agentId: "searchAgent",
        render: ({ state }) => (
          <div>
            {state.searches?.map((search, index) => (
              <div key={index}>
                {search.done ? "✅" : "❌"} {search.query}{search.done ? "" : "..."}
              </div>
            ))}
          </div>
        ),
      });

      // ...

      return <div>...</div>;
    }
    ```

    <Callout type="warn" title="重要">
      `name` 参数必须与你 Strands 配置中定义的 Agent 名称完全匹配（例如，上面的 `searchAgent`）。
    </Callout>

  </Step>
  <Step>
    ### 在聊天外渲染状态

    你还可以在**聊天外**渲染 Agent 的状态。当你想在聊天之外的任何地方渲染 Agent 的状态时，这很有用。

    ```tsx title="app/page.tsx"
    import { useAgent } from "@copilotkit/react-core/v2"; // [!code highlight]
    // ...

    // 定义 Agent 的状态，应与你的 Strands Agent 的状态 schema 匹配
    type AgentState = {
      searches: {
        query: string;
        done: boolean;
      }[];
    };

    function YourMainContent() {
      // ...

      // [!code highlight:3]
      const { agent } = useAgent({
        agentId: "searchAgent",
      })

      // ...

      return (
        <div>
          {/* ... */}
          <div className="flex flex-col gap-2 mt-4">
            {/* [!code highlight:5] */}
            {agent.state?.searches?.map((search, index) => (
              <div key={index} className="flex flex-row">
                {search.done ? "✅" : "❌"} {search.query}
              </div>
            ))}
          </div>
        </div>
      )
    }
    ```

    <Callout type="warn" title="重要">
      `name` 参数必须与你 Strands 配置中定义的 Agent 名称完全匹配（例如，上面的 `searchAgent`）。
    </Callout>

  </Step>
  <Step>
    ### 试试看！

    你现在创建了一个将在聊天中渲染 Agent 状态的组件。

    <video
      src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/agentic-generative-ui.mp4"
      className="rounded-lg shadow-xl"
      loop
      playsInline
      controls
      autoPlay
      muted
    />
  </Step>
</Steps>
