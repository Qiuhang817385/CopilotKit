---
title: "MCP (智能体<->工具)"
description: "将 Model Context Protocol (MCP) 服务器集成到您的 React 应用程序中"
icon: "lucide/Network"
---

import Image from "next/image"
import {
  TailoredContent,
  TailoredContentOption,
} from "@/components/react/tailored-content.tsx";
import Link from "next/link";
import {
  Database,
  Code,
  Server,
  Bot,
  SquareTerminal,
  SquareChartGantt,
} from "lucide-react";

## 简介

Model Context Protocol 是一个开放标准，使开发人员能够在其数据源和 AI 驱动工具之间建立安全、双向的连接。通过 MCP，您可以：

- 将 AI 应用程序连接到您的数据源
- 使 AI 工具能够安全地访问和利用您的数据
- 构建具有应用程序上下文的 AI 驱动功能

如需进一步阅读，请查看 [Model Context Protocol](https://modelcontextprotocol.io/introduction) 网站。

<Callout type="info" title="寻找 MCP 应用？">
  如果您希望 MCP 服务器返回**直接在聊天中渲染的交互式 UI 组件**，请查看 [MCP 应用](/learn/generative-ui/specs/mcp-apps)。
</Callout>

<Image 
  src="/images/any-agentic-backend-light.png" 
  alt="CopilotKit 与智能体协议" 
  width={4096} 
  height={2304}
  className="block dark:hidden w-full mx-auto"
/>
<Image 
  src="/images/any-agentic-backend-dark.png" 
  alt="CopilotKit 与智能体协议" 
  width={4096} 
  height={2304}
  className="hidden dark:block w-full mx-auto"
/>
<div className="text-center text-sm text-muted-foreground mt-2 mb-8 w-3/5 mx-auto">
  MCP 是 CopilotKit 支持的三大[智能体协议](/agentic-protocols)之一，用于将智能体连接到面向用户的前端
</div>

## 与 CopilotKit 快速入门

<Steps>
  <Step>
    ### 获取 MCP 服务器
    首先，我们需要确保有一个 MCP 服务器可以连接。您可以使用任何已配置的 MCP SSE 端点。

    <Accordions className="shadow-lg ring-1 ring-indigo-400 selected bg-gradient-to-r from-indigo-100/80 to-purple-200 dark:from-indigo-900/30 dark:to-purple-900/50">
      <Accordion title="从 Composio 获取 MCP 服务器">
        <p>
          Composio 提供了一个现成的 MCP 服务器注册表，具有简单的认证和设置。

          要开始使用，请前往 [Composio](https://mcp.composio.dev/)，找到适合您需求的服务器并复制 SSE URL，然后继续此处。
        </p>
      </Accordion>
    </Accordions>

  </Step>
  <TailoredContent
    className="step"
    id="cli"
    header={
        <div>
            <p className="text-xl font-semibold">使用 CopilotKit CLI</p>
        </div>
    }
  >
    <TailoredContentOption
      id="use-cli"
      title="使用 CopilotKit CLI"
      description="我有一个 Next.js 应用程序，想快速开始。"
      icon={<SquareTerminal />}
    >
      <Step>
        ### 运行 CLI
        只需在您的 Next.js 应用程序中运行以下命令即可开始！

        <Accordions>
            <Accordion title="还没有 Next.js 应用程序？">
                没问题！只需使用 `create-next-app` 快速创建一个。
                ```bash
                npx create-next-app@latest
                ```
            </Accordion>
        </Accordions>

        ```bash
        npx copilotkit@latest init -m MCP
        ```
      </Step>
    </TailoredContentOption>
    <TailoredContentOption
        id="do-it-manually"
        title="跟着代码学习"
        description="我想深入了解底层原理，或者我没有 Next.js 应用程序。"
        icon={<SquareChartGantt />}
    >
      <Step>
        #### 设置 CopilotKit Provider

        用 `CopilotKit` provider 包装您的应用程序：

        ```tsx
        "use client";

        import { CopilotKit } from "@copilotkit/react-core";

        export default function App() {
          return (
            <CopilotKit publicApiKey="<replace_with_your_own>">
              {/* 您的应用内容 */}
            </CopilotKit>
          );
        }
        ```
      </Step>
      <Step>
        #### 连接到 MCP 服务器

        创建一个组件来管理 MCP 服务器连接：

        ```tsx
        "use client";

        import { useCopilotKit } from "@copilotkit/react-core/v2";
        import { useEffect } from "react";

        function McpServerManager() {
          const { setMcpServers } = useCopilotKit();

          useEffect(() => {
            setMcpServers([
              {
                // 在 https://mcp.composio.dev/ 尝试一个示例 MCP 服务器
                endpoint: "your_mcp_sse_url",
              },
            ]);
          }, [setMcpServers]);

          return null;
        }

        export default McpServerManager;

        ```
      </Step>
      <Step>
        #### 添加聊天界面

        将 `CopilotChat` 组件添加到您的页面：

        ```tsx
        "use client";

        import { CopilotChat } from "@copilotkit/react-core/v2";
        import McpServerManager from "./McpServerManager";

        export default function ChatInterface() {
          return (
            <div className="flex h-screen p-4">
              <McpServerManager />
              <CopilotChat
                className="flex-grow rounded-lg w-full"
              />
            </div>
          );
        }
        ```
      </Step>
      <Step>
        #### 可视化 MCP 工具调用（可选）

        创建一个组件来在您的 UI 中显示 MCP 工具调用：

        ```tsx
        "use client";

        import {
          useFrontendTool,
          CatchAllActionRenderProps,
        } from "@copilotkit/react-core/v2";
        import McpToolCall from "./McpToolCall";

        export function ToolRenderer() {
          useFrontendTool({
            /**
             * 星号 (*) 匹配所有工具调用
             */
            name: "*",
            render: ({ name, status, args, result }: CatchAllActionRenderProps<[]>) => (
              <McpToolCall status={status} name={name} args={args} result={result} />
            ),
          });
          return null;
        }
        ```
      </Step>
      <Step>
        #### 完整实现

        将所有组件组合在一起：

        ```tsx
        "use client";

        import { CopilotKit } from "@copilotkit/react-core";
        import { CopilotChat } from "@copilotkit/react-core/v2";
        import McpServerManager from "./McpServerManager";
        import { ToolRenderer } from "./ToolRenderer";

        export default function Page() {
          return (
            <CopilotKit publicApiKey="<replace_with_your_own>">
              <div className="flex h-screen p-4">
                <McpServerManager />
                <CopilotChat
                  className="flex-grow rounded-lg w-full"
                />
                <ToolRenderer />
              </div>
            </CopilotKit>
          );
        }
        ```
      </Step>
    </TailoredContentOption>

  </TailoredContent>
</Steps>

## 高级用法

### 实现 McpToolCall 组件

<details>
<summary>点击查看 McpToolCall 组件实现</summary>

```tsx
"use client";

import * as React from "react";

interface ToolCallProps {
  status: "complete" | "inProgress" | "executing";
  name?: string;
  args?: any;
  result?: any;
}

export default function MCPToolCall({
  status,
  name = "",
  args,
  result,
}: ToolCallProps) {
  const [isOpen, setIsOpen] = React.useState(false);

  // 格式化内容以供显示
  const format = (content: any): string => {
    if (!content) return "";
    const text =
      typeof content === "object"
        ? JSON.stringify(content, null, 2)
        : String(content);
    return text
      .replace(/\\n/g, "\n")
      .replace(/\\t/g, "\t")
      .replace(/\\"/g, '"')
      .replace(/\\\\/g, "\\");
  };

  return (
    <div className="bg-[#1e2738] rounded-lg overflow-hidden w-full">
      <div
        className="p-3 flex items-center cursor-pointer"
        onClick={() => setIsOpen(!isOpen)}
      >
        <span className="text-white text-sm overflow-hidden text-ellipsis">
          {name || "MCP 工具调用"}
        </span>
        <div className="ml-auto">
          <div
            className={`w-2 h-2 rounded-full ${
              status === "complete"
                ? "bg-gray-300"
                : status === "inProgress" || status === "executing"
                ? "bg-gray-500 animate-pulse"
                : "bg-gray-700"
            }`}
          />
        </div>
      </div>

      {isOpen && (
        <div className="px-4 pb-4 text-gray-300 font-mono text-xs">
          {args && (
            <div className="mb-4">
              <div className="text-gray-400 mb-2">参数：</div>
              <pre className="whitespace-pre-wrap max-h-[200px] overflow-auto">
                {format(args)}
              </pre>
            </div>
          )}

          {status === "complete" && result && (
            <div>
              <div className="text-gray-400 mb-2">结果：</div>
              <pre className="whitespace-pre-wrap max-h-[200px] overflow-auto">
                {format(result)}
              </pre>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
```

</details>

### 自托管选项

<details>
<summary>点击这里了解如何将 MCP 与自托管运行时一起使用</summary>

<Callout type="info" title="自托管 vs Copilot Cloud">
  Copilot Runtime 处理与 LLM 的通信、消息历史记录和
  状态。您可以自托管它或使用{" "}
  <LinkToCopilotCloud asButton={false}>Copilot Cloud</LinkToCopilotCloud>{" "}
  （推荐）。在我们的[自托管指南](/built-in-agent/copilot-runtime)中了解更多。
</Callout>

要将您的自托管运行时配置为 MCP 服务器，您需要实现与此接口匹配的 `createMCPClient` 函数：

```typescript
type CreateMCPClientFunction = (
  config: MCPEndpointConfig
) => Promise<MCPClient>;
```

有关详细的实现指南，请参阅 [官方 MCP SDK 文档](https://github.com/modelcontextprotocol/typescript-sdk?tab=readme-ov-file#writing-mcp-clients)。

以下是配置运行时的基本示例：

```tsx
import {
  CopilotRuntime,
  OpenAIAdapter,
  copilotRuntimeNextJSAppRouterEndpoint,
} from "@copilotkit/runtime";
import { NextRequest } from "next/server";

const serviceAdapter = new OpenAIAdapter();

const runtime = new CopilotRuntime({
  createMCPClient: async (config) => {
    // 在此处实现您的 MCP 客户端创建逻辑
    // 有关实现细节，请参阅 MCP SDK 文档
  },
});

export const POST = async (req: NextRequest) => {
  const { handleRequest } = copilotRuntimeNextJSAppRouterEndpoint({
    runtime,
    serviceAdapter,
    endpoint: "/api/copilotkit",
  });

  return handleRequest(req);
};
```

</details>
