---
title: "MCP 应用"
icon: "lucide/Monitor"
description: "直接在您的聊天界面中渲染来自 MCP 服务器的交互式 UI 组件。"
hideTOC: true
---
import { IntegrationGrid } from "@/components/content/integration-grid";

## 这是什么？

MCP 应用是带有相关 UI 资源的工具暴露型 MCP 服务器。当智能体调用这些工具之一时，CopilotKit 会自动获取并在聊天中渲染 UI 组件 —— 无需额外的前端代码。

主要优势：
- **零前端代码** — UI 组件由 MCP 服务器提供
- **完全交互性** — 组件可以使用 HTML、CSS 和 JavaScript
- **安全沙箱** — 内容在隔离的 iframe 中运行
- **线程持久化** — MCP 应用存储在对话历史中，重新连接时恢复

## 选择您的 AI 后端

<IntegrationGrid path="generative-ui/mcp-apps" />
