---
title: 展示组件
icon: "lucide/Eye"
description: 注册可在聊天中渲染的 React 组件。
hideTOC: true
---
import { IntegrationGrid } from "@/components/content/integration-grid";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

## 这是什么？

纯展示生成式 UI 允许您将 React 组件注册为 Agent 可调用的工具。当 Agent 调用该工具时，CopilotKit 会将您的组件直接渲染在聊天中，工具的参数作为 props 传入——无需处理逻辑或用户交互。

<Tabs items={['page.tsx', 'chart.tsx']}>
  <Tab value="page.tsx">
  ```tsx
  import { z } from "zod";
  import { useComponent } from "@copilotkit/react-core/v2";
  import { ChartProps, Chart } from "@/chart.tsx"

  useComponent({
    name: "showChart",
    description: "填充数据并向用户展示图表",
    parameters: ChartProps,
    render: Chart
  });
  ```
  </Tab>
  <Tab value="chart.tsx">
  ```tsx
  import { z } from "zod";
  import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";

  export const ChartProps = z.object({
    title: z.string(),
    data: z.array(z.object({ label: z.string(), value: z.number() })),
  });

  export function Chart({ title, data }: z.infer<typeof ChartProps>) {
    return (
      <div>
        <h3>{title}</h3>
        <ResponsiveContainer width="100%" height={300}>
          <BarChart data={data}>
            <XAxis dataKey="label" /><YAxis /><Tooltip />
            <Bar dataKey="value" fill="#6366f1" />
          </BarChart>
        </ResponsiveContainer>
      </div>
    );
  }
  ```
  </Tab>
</Tabs>

## 何时使用？

在以下场景中使用纯展示生成式 UI：
- 在聊天中展示丰富的 UI（卡片、图表、表格）
- 展示 Agent 响应中的结构化数据
- 渲染预览、状态指示器或视觉反馈
- 让 Agent 呈现超越纯文本的信息

## 选择您的 AI 后端

<IntegrationGrid path="generative-ui/your-components/display-only" />
