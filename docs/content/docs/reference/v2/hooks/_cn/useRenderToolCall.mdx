---
title: "useRenderToolCall"
description: "返回用于聊天界面中工具调用渲染函数的 React Hook"
---

## 概述

`useRenderToolCall` 返回一个渲染函数，该函数将工具调用映射到 React 元素。返回的函数通过工具名称查找第一个匹配的渲染配置（如果未找到精确匹配，则回退到通配符 `"*"` 渲染器），解析 JSON 参数，确定当前的 `ToolCallStatus`，并返回相应的 React 元素——如果未注册该工具的渲染器，则返回 `null`。

此 Hook 主要由聊天 UI 组件用于显示工具调用的视觉反馈。在大多数应用程序中，你不会直接调用它；相反，你通过 `useFrontendTool` 或 `useHumanInTheLoop` 注册渲染组件，聊天组件会在内部使用 `useRenderToolCall` 来解析它们。

## 签名

```tsx
import { useRenderToolCall } from "@copilotkit/react-core/v2";

function useRenderToolCall(): (props: UseRenderToolCallProps) => React.ReactElement | null;
```

## 返回值

<PropertyReference name="renderer" type="(props: UseRenderToolCallProps) => React.ReactElement | null">
  一个接受工具调用 props 并返回 React 元素或 `null` 的函数。

  <PropertyReference name="props" type="UseRenderToolCallProps" required>
    描述要渲染的工具调用的输入对象。

    <PropertyReference name="toolCall" type="ToolCall" required>
      包含工具 `name` 及其 JSON 编码的 `args` 的工具调用对象。
    </PropertyReference>

    <PropertyReference name="toolMessage" type="ToolMessage">
      工具结果消息（如果有）。存在时表示工具调用已完成并包含结果字符串。
    </PropertyReference>
  </PropertyReference>
</PropertyReference>

## 状态解析

渲染器根据输入确定 `ToolCallStatus`：

| 条件 | 状态 | `result` |
|-----------|--------|----------|
| 没有 `toolMessage` 且工具调用仍在加载中 | `ToolCallStatus.InProgress` | `undefined` |
| 工具调用正在执行（参数已解析，尚无结果） | `ToolCallStatus.Executing` | `undefined` |
| 存在匹配的 `toolMessage` | `ToolCallStatus.Complete` | 来自工具消息的结果字符串 |

## ToolCallStatus

`ToolCallStatus` 枚举从 `@copilotkit/react-core/v2` 导出：

| 值 | 描述 |
|-------|-------------|
| `ToolCallStatus.InProgress` | 工具调用的参数仍在流式传输中。 |
| `ToolCallStatus.Executing` | 参数已完全解析；工具正在执行。 |
| `ToolCallStatus.Complete` | 执行完成且结果可用。 |

## 用法

### 在自定义聊天组件中使用渲染器

```tsx
import { useRenderToolCall } from "@copilotkit/react-core/v2";

function CustomChatMessage({ message }) {
  const renderToolCall = useRenderToolCall();

  if (message.type === "tool_call") {
    const element = renderToolCall({
      toolCall: message.toolCall,
      toolMessage: message.toolMessage,
    });

    if (element) {
      return <div className="tool-call-container">{element}</div>;
    }

    return <div className="text-gray-400">Tool call: {message.toolCall.name}</div>;
  }

  return <div>{message.content}</div>;
}
```

### 使用 useFrontendTool 注册渲染器

工具调用渲染器通常通过 `useFrontendTool` 或直接通过 Provider 上的 `renderToolCalls` 属性注册。`useRenderToolCall` Hook 在渲染时解析这些注册。

```tsx
import { z } from "zod";
import { useFrontendTool, ToolCallStatus } from "@copilotkit/react-core/v2";

function App() {
  // 注册带有渲染组件的工具
  useFrontendTool({
    name: "searchDatabase",
    description: "Search the product database",
    parameters: z.object({
      query: z.string().describe("Search query"),
    }),
    handler: async ({ query }) => {
      const results = await fetch(`/api/search?q=${query}`);
      return JSON.stringify(await results.json());
    },
    render: ({ args, status, result }) => {
      if (status === ToolCallStatus.InProgress) {
        return <div>Searching for "{args.query}"...</div>;
      }
      if (status === ToolCallStatus.Complete && result) {
        const data = JSON.parse(result);
        return (
          <div>
            <p>Found {data.length} results for "{args.query}"</p>
            <ul>
              {data.map((item: any) => (
                <li key={item.id}>{item.name}</li>
              ))}
            </ul>
          </div>
        );
      }
      return null;
    },
  }, []);

  return <ChatInterface />;
}
```

### 通配符渲染器

如果未找到精确的名称匹配，Hook 会回退到通配符 `"*"` 渲染器。这对于为所有未处理的工具调用提供通用 UI 很有用。

```tsx
import { CopilotKitProvider } from "@copilotkit/react-core/v2";
import { ToolCallStatus } from "@copilotkit/react-core/v2";

function App() {
  return (
    <CopilotKitProvider
      runtimeUrl="/api/copilotkit"
      renderToolCalls={[
        {
          name: "*",
          render: ({ name, args, status, result }) => {
            if (status === ToolCallStatus.InProgress) {
              return (
                <div className="text-gray-500 text-sm">
                  Running {name}...
                </div>
              );
            }
            if (status === ToolCallStatus.Complete) {
              return (
                <div className="text-green-600 text-sm">
                  {name} completed.
                </div>
              );
            }
            return null;
          },
        },
      ]}
    >
      <YourApp />
    </CopilotKitProvider>
  );
}
```

## 行为

- **基于名称的查找**：渲染器首先搜索已注册的渲染配置以查找精确的工具名称匹配，然后回退到通配符 (`"*"`) 渲染器。
- **JSON 解析**：工具调用的 `args` 字符串在传递给渲染组件之前从 JSON 解析。如果解析失败，args 默认为空对象。
- **状态推断**：状态由 `toolMessage` prop 的存在和状态决定。渲染组件始终接收具有一致形状的 `name`、`args`、`status` 和 `result`。
- **对未匹配工具返回 null**：如果没有为工具名称注册渲染器且不存在通配符渲染器，函数返回 `null`。
- **由聊天组件内部使用**：内置的 `CopilotChat`、`CopilotPopup` 和 `CopilotSidebar` 组件使用此 Hook 来渲染工具调用。只有在你构建自定义聊天 UI 时才需要直接调用它。

## 相关

- [`useFrontendTool`](/reference/v2/hooks/useFrontendTool) -- 注册带有渲染组件的工具
- [`useHumanInTheLoop`](/reference/v2/hooks/useHumanInTheLoop) -- 注册带有渲染组件的交互式工具
- [`CopilotKitProvider`](/reference/v2) -- 在 Provider 级别配置静态渲染工具调用
