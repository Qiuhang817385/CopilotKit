---
title: 写入 Agent 状态
icon: "lucide/ArrowRight"
description: 从您的应用程序向 Agent 的状态写入数据。
---
import { ImageZoom } from 'fumadocs-ui/components/image-zoom';
import RunAndConnect from "@/snippets/integrations/mastra_cn/run-and-connect.mdx"
import { IframeSwitcher } from "@/components/content"

<IframeSwitcher
  id="shared-state-write-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/mastra/feature/shared_state?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/mastra/feature/shared_state?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout type="info">
  此示例演示了在 [CopilotKit 功能查看器](https://feature-viewer.copilotkit.ai/mastra/feature/shared_state) 中写入共享状态。
</Callout>

## 这是什么？

您可以轻松地从原生应用程序向您的 Agent 状态写入数据，从而允许您从 UI 更新 Agent 的工作内存。

<Callout type="info" title="重要">
  本指南假设您将 Mastra Agent 嵌入到 Copilot Runtime 中，如下所示。

  ```ts
  const runtime = new CopilotRuntime({
    agents: MastraAgent.getLocalAgents({ mastra }),
  });
  ```

  如果您使用的是**远程 Mastra Agent**，则此功能**将无法工作**。
</Callout>

## 何时使用？

当您想要向 Agent 的工作内存提供用户输入或控制时，可以使用此功能。随着应用程序状态的变化，您可以更新 Agent 状态以反映这些变化。

## 实现

<Steps>
  <Step>
    ### 运行并连接您的 Agent
    <RunAndConnect components={props.components} />
  </Step>
  <Step>
    ### 定义 Agent 状态
    Mastra 具有高级的[工作内存概念](https://mastra.ai/en/docs/memory/working-memory)，可为您的 Agent 提供状态保持能力。CopilotKit 利用 Mastra 的工作内存概念，允许您在 Agent 和应用程序之间实现共享状态。

    为 Agent 提供工作内存非常简单，只需向 Agent 提供一个 Zod schema 即可。

    ```ts title="mastra/agents/language-agent.ts"
    import { openai } from "@ai-sdk/openai";
    import { Agent } from "@mastra/core/agent";
    import { LibSQLStore } from "@mastra/libsql";
    import { z } from "zod";
    import { Memory } from "@mastra/memory";

    // [!code highlight:4]
    // 1. 定义 Agent 状态 schema
    export const AgentStateSchema = z.object({
      language: z.enum(["english", "spanish"]),
    });

    // 2. 从 schema 推断 Agent 状态类型
    export const AgentState = z.infer<typeof AgentStateSchema>;

    // 3. 创建 Agent
    export const languageAgent = new Agent({
      name: "Language Agent",
      model: openai("gpt-5.2"),
      instructions: "Always communicate in the preferred language of the user as defined in your working memory. Do not communicate in any other language.",
      memory: new Memory({
        storage: new LibSQLStore({ id: "mastra-storage", url: ":memory:" }),
        options: {
          // [!code highlight:4]
          workingMemory: {
            enabled: true,
            schema: AgentStateSchema,
          },
        },
      }),
    });
    ```
  </Step>
  <Step>
    ### 使用 `useAgent` Hook
    在您的 Agent 连接并运行后，剩下的就是调用 `useAgent` hook，传入 Agent 的名称，并使用 `agent.setState` 来更新 Agent 状态。

    ```tsx title="ui/app/page.tsx"
    import { useAgent } from "@copilotkit/react-core/v2"; // [!code highlight]
    import { AgentState } from "@/mastra/agents/language-agent";

    function YourMainContent() {
      // [!code highlight:5]
      const { agent } = useAgent({
        agentId: "your-mastra-agent-name",
        // 可选：提供类型安全的初始状态
        initialState: { language: "english" }
      });

      const toggleLanguage = () => {
        agent.setState({ language: agent.state?.language === "english" ? "spanish" : "english" }); // [!code highlight]
      };

      return (
        // 为简洁起见，省略了样式
        <div>
          <h1>Your main content</h1>
          {/* [!code highlight:2] */}
          <p>Language: {agent.state?.language}</p>
          <button onClick={toggleLanguage}>Toggle Language</button>
        </div>
      );
    }
    ```
    <Callout type="info">
      `useAgent` 中的 `agent.setState` 函数将更新工作内存，并在状态变化时触发重新渲染。
    </Callout>
  </Step>
  <Step>
    ### 试一试！
    您现在可以使用 `setState` 函数来更新 Agent 状态，并使用 `state` 来读取它。尝试切换语言按钮并与您的 Agent 对话。您会看到语言会根据 Agent 的状态而改变。

    <Callout type="info" title="重要">
      Mastra 中的共享状态是基于提示驱动的。这意味着您的 Agent 对共享状态的了解将通过您提供给 Agent 的指令来增强。
    </Callout>
  </Step>
</Steps>
