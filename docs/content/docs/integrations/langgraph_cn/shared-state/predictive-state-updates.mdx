---
title: "状态流式传输"
icon: "lucide/Podcast"
description: 将进行中的智能体状态更新流式传输到前端。
---
import {
  TailoredContent,
  TailoredContentOption,
} from "@/components/react/tailored-content.tsx";
import { CoAgentsEnterpriseCTA } from "@/components/react/coagents/coagents-enterprise-cta.tsx";
import InstallSDKSnippet from "@/snippets/_cn/install-sdk.mdx"
import { FaWrench } from "react-icons/fa";
import { FaArrowUp } from "react-icons/fa";
import { IframeSwitcher } from "@/components/content"

<IframeSwitcher
  id="predictive-state-updates-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/predictive_state_updates?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/predictive_state_updates?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout type="info">
  此示例演示了 [CopilotKit 功能查看器](https://feature-viewer.copilotkit.ai/langgraph/feature/predictive_state_updates) 中的预测状态更新。
</Callout>

## 这是什么？

LangGraph 智能体的状态更新是不连续的；仅在图中的节点转换之间发生。
但即使是图中的_单个节点_通常也需要运行很多秒，并且包含用户感兴趣的子步骤。

**智能体原生应用程序**向终端用户反映智能体正在做什么_尽可能连续地_。

CopilotKit 通过其_**预测状态更新**_的概念实现了这一点。


## 何时应该使用？

当您想向用户提供有关智能体正在做什么的反馈时，您可以使用此功能，特别是为了：

- **保持用户参与**，避免长时间加载指示器
- **建立信任**，通过展示智能体正在做什么
- 启用 **智能体引导** - 允许用户在需要时纠正智能体的方向

## 重要提示

当 LangGraph 中的节点完成执行时，**其返回的状态将成为唯一真实来源**。虽然中间状态更新非常适合实时反馈，但您希望持久化的任何更改必须明确包含在节点的最终返回状态中。否则，当节点完成时它们将被覆盖。


## 实现

<Steps>
    <Step>
        ### 安装 CopilotKit SDK
        <InstallSDKSnippet components={props.components}/>
    </Step>
    <Step>
        ### 定义状态
        我们将在状态中定义一个 `observed_steps` 字段，当智能体写入报告的不同部分时，它将被更新。

        <Tabs groupId="language_langgraph_agent" items={["Python", "TypeScript"]} persist>
            <Tab value="Python">
                ```python title="agent.py"
                from copilotkit import CopilotKitState
                from typing import Literal

                class AgentState(CopilotKitState):
                    observed_steps: list[str]  # 已完成步骤的数组
                ```
            </Tab>
            <Tab value="TypeScript">
                ```ts title="agent-js/src/agent.ts"
                import { Annotation } from "@langchain/langgraph";
                import { CopilotKitStateAnnotation } from "@copilotkit/sdk-js/langgraph";

                export const AgentStateAnnotation = Annotation.Root({
                    observed_steps: Annotation<string[]>,  // 已完成步骤的数组
                    ...CopilotKitStateAnnotation.spec,
                });
                export type AgentState = typeof AgentStateAnnotation.State;
                ```
            </Tab>
        </Tabs>
    </Step>
    <Step>
        ### 发出中间状态
        <TailoredContent
            id="state-emission"
            header={
                <div>
                    <p className="text-xl font-semibold">您希望如何发出状态更新？</p>
                    <p className="text-base">
                        您可以手动发出状态更新，或将特定的工具调用配置为自动发出更新。
                    </p>
                </div>
            }
        >
            <TailoredContentOption
                id="manual-emission"
                title="手动预测状态更新"
                description="手动发出状态更新，以最大限度地控制更新发生的时间。"
                icon={<FaArrowUp />}
            >
                对于长时间运行的任务，您可以逐步发出状态更新，作为最终状态的预测。在此示例中，我们通过在每个更新之间延迟一秒钟来模拟长时间运行的任务。
                <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
                    <Tab value="Python">
                        ```python title="agent.py"
                        from copilotkit.langgraph import copilotkit_emit_state # [!code highlight]
                        # ...
                        async def chat_node(state: AgentState, config: RunnableConfig) -> Command[Literal["cpk_action_node", "tool_node", "__end__"]]:
                            # ...

                            # 模拟逐个执行步骤
                            steps = [
                                "正在分析输入数据...",
                                "正在识别关键模式...",
                                "正在生成建议...",
                                "正在格式化最终输出..."
                            ]

                            for step in steps:
                                self.state["observed_steps"] = self.state.get("observed_steps", []) + [step]
                                await copilotkit_emit_state(config, state) # [!code highlight]
                                await asyncio.sleep(1)

                            # ...
                        ```
                    </Tab>
                    <Tab value="TypeScript">
                        ```ts title="agent-js/src/agent.ts"
                        import { copilotkitEmitState } from "@copilotkit/sdk-js/langgraph"; // [!code highlight]
                        // ...
                        async function chat_node(state: AgentState, config: RunnableConfig) {
                            // ...

                            // 模拟逐个执行步骤
                            const steps = [
                                "正在分析输入数据...",
                                "正在识别关键模式...",
                                "正在生成建议...",
                                "正在格式化最终输出..."
                            ];

                            for (const step of steps) {
                                state.observed_steps = [...(state.observed_steps ?? []), step];
                                copilotkitEmitState(config, state);
                                await new Promise(resolve => setTimeout(resolve, 1000));
                            }
                        }
                        ```
                    </Tab>
                </Tabs>
            </TailoredContentOption>

            <TailoredContentOption
                id="tool-emission"
                title="基于工具的预测状态更新"
                description="将特定的工具调用配置为自动发出中间状态更新。"
                icon={<FaWrench />}
            >
                对于长时间运行的任务，您可以将 CopilotKit 配置为在调用特定工具时自动预测状态更新。在此示例中，我们将配置 CopilotKit 在 LLM 调用步骤进度工具时预测状态更新。
                <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
                    <Tab value="Python">
                        ```python
                        from copilotkit.langgraph import copilotkit_customize_config
                        from copilotkit import CopilotKitState
                        from langgraph.types import Command
                        from langgraph.graph import END
                        from langchain.tools import tool
                        from langchain_openai import ChatOpenAI
                        from langchain_core.messages import SystemMessage, AIMessage
                        from langchain_core.runnables import RunnableConfig

                        # 定义一个步骤进度工具供 LLM 报告步骤
                        @tool
                        def step_progress_tool(steps: list[str])
                            """读取并报告步骤"""

                        async def frontend_actions_node(state: AgentState, config: RunnableConfig):
                            # 配置 CopilotKit 将步骤进度工具调用视为最终状态的预测
                            config = copilotkit_customize_config(
                                config,
                                emit_intermediate_state=[
                                    {
                                        "state_key": "observed_steps",
                                        "tool": "step_progress_tool",
                                        "tool_argument": "steps"
                                    },
                                ]
                            )

                            system_message = SystemMessage(
                                content=f"你是一个任务执行者。假装执行给定的任务，使用 step_progress_tool 报告步骤。"
                            )

                            # 向 LLM 提供操作
                            model = ChatOpenAI(model="gpt-4").bind_tools(
                                [
                                    *state["copilotkit"]["actions"],
                                    step_progress_tool
                                    # 您的其他工具在这里
                                ],
                            )

                            # 使用 CopilotKit 修改后的配置调用模型
                            response = await model.ainvoke([
                                system_message,
                                *state["messages"],
                            ], config)

                            # 在状态中设置步骤，以便它们被持久化并传达给前端
                            if isinstance(response, AIMessage) and response.tool_calls and response.tool_calls[0].get("name") == 'step_progress_tool':
                                return Command(
                                    goto=END,
                                    update={
                                        "messages": response,
                                        "observed_steps": response.tool_calls[0].get("args", None).get('steps')
                                    }
                                )

                            return Command(goto=END, update={"messages": response})
                        ```
                    </Tab>
                    <Tab value="TypeScript">
                        ```typescript
                        import { copilotkitCustomizeConfig } from '@copilotkit/sdk-js/langgraph';

                        async function frontendActionsNode(state: AgentState, config: RunnableConfig): Promise<AgentState> {
                            const modifiedConfig = copilotkitCustomizeConfig(config, {
                                emitIntermediateState: [
                                {
                                    stateKey: "observed_steps",
                                    tool: "StepProgressTool",
                                    toolArgument: "steps",
                                },
                                ],
                            });

                            const stepProgress = tool(
                                async (args) => args,
                                {
                                    name: "StepProgressTool",
                                    description: "通过更新步骤数组记录进度",
                                    schema: z.object({
                                        steps: z.array(z.string()),
                                    }),
                                }
                            );

                            const model = new ChatOpenAI({
                                model: "gpt-5.2",
                            }).bindTools([stepProgress]);

                            const system_message = new SystemMessage("你是一个任务执行者。假装执行给定的任务，使用 StepProgressTool 报告步骤。")
                            const response = await model.invoke([system_message, ...state.messages], modifiedConfig);


                            if (response.tool_calls?.length) {
                                return {
                                    messages: response;
                                    observed_steps: response.tool_calls[0].args.steps,
                                }

                            return { messages: response };
                        }
                        ```
                    </Tab>
                </Tabs>
            </TailoredContentOption>
        </TailoredContent>
    </Step>
    <Step>
        ### 观察预测
        这些预测将在智能体运行时发出，允许您在确定最终状态之前跟踪其进度。

        ```tsx title="ui/app/page.tsx"
        import { useAgent } from '@copilotkit/react-core/v2'; // [!code highlight]

        const YourMainContent = () => {
            // [!code highlight:3]
            const { agent } = useAgent({
                agentId: "sample_agent",
            });

            const observedSteps = (agent.state.observed_steps as string[]) ?? [];

            return (
                <div>
                    <h1>智能体进度</h1>
                    {observedSteps.length > 0 && (
                        <div>
                            <h3>步骤：</h3>
                            <ul>
                                {observedSteps.map((step, i) => (
                                    <li key={i}>{step}</li>
                                ))}
                            </ul>
                        </div>
                    )}
                </div>
            )
        }
        ```
    </Step>
    <Step>
        ### 试试看！
        现在您会注意到状态预测在智能体取得进展时发出，让您在确定最终状态之前了解其工作情况。
        您可以将此模式应用于智能体中的任何长时间运行的任务。

        <video src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/intermediate-state-render.mp4" className="rounded-lg shadow-xl" loop playsInline controls autoPlay muted />
        <Callout>
          此视频展示了将 [实现](#实现) 部分应用于 `npx copilotkit@latest init` 的结果！
        </Callout>
    </Step>
</Steps>
