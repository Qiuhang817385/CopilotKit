---
title: 输入/输出模式
icon: "lucide/Play"
description: 决定哪些状态属性被接收并返回给前端
---

## 这是什么？

并非所有状态属性都与前后端共享相关。
本指南展示如何确保只有正确的状态部分被来回传递。

本指南基于 [LangGraph 的输入/输出模式功能](https://docs.langchain.com/oss/python/langgraph/use-graph-api#define-input-and-output-schemas)

## 何时应该使用？

根据您的实现，某些属性是用于内部处理的，而另一些则是 UI 与用户通信的方式。
此外，某些状态属性包含大量信息。在智能体和 UI 之间同步它们可能成本高昂，而且可能没有实际好处。

## 实现

<Steps>
  <Step>
      ### 检查我们旧的状态
      LangGraph 是有状态的。当您在节点之间转换时，该状态会被更新并传递给下一个节点。在此示例中，
      让我们假设我们的智能体应该使用的状态可以描述如下：
      <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
        <Tab value="Python">
        ```python title="agent.py"
        from copilotkit import CopilotKitState
        from typing import Literal

        class AgentState(CopilotKitState):
            question: str
            answer: str
            resources: List[str]
        ```
        </Tab>
        <Tab value="TypeScript">
        ```typescript title="agent-js/sample_agent/agent.ts"
        import { Annotation } from "@langchain/langgraph";
        import { CopilotKitStateAnnotation } from "@copilotkit/sdk-js/langgraph";

        const AgentState = Annotation.Root({
          ...CopilotKitStateAnnotation.spec,
          question: Annotation<string>,
          answer: Annotation<string>,
          resources: Annotation<string[]>,
        })
        ```
        </Tab>
    </Tabs>
  </Step>
  <Step>
    ### 将状态划分为输入和输出
    我们的示例列出了几个状态属性，每个都有其自己的用途：
      - 问题是用户提出的，期望 LLM 回答
      - 答案是 LLM 返回的内容
      - 资源列表将被 LLM 用于回答问题，不应传达给用户或由他们设置。

      <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
          <Tab value="Python">
          ```python title="agent.py"
          from copilotkit import CopilotKitState
          from typing import Literal

          # 将状态分为 3 部分

          # 输入模式，用于您愿意从前端接受的输入
          class InputState(CopilotKitState):
            question: str

          # 输出模式，用于您愿意传递给前端的输出
          class OutputState(CopilotKitState):
            answer: str

          # 完整模式，包括输入、输出和内部状态（在我们的例子中是"resources"）
          class OverallState(InputState, OutputState):
            resources: List[str]

          async def answer_node(state: OverallState, config: RunnableConfig):
            """
            标准聊天节点，用于回答一般问题。
            """

            model = ChatOpenAI()

            # 将输入问题添加到系统提示中，以便传递给 LLM
            system_message = SystemMessage(
              content=f"你是一个有用的助手。回答问题：{state.get('question')}"
            )

            response = await model.ainvoke([
              system_message,
              *state["messages"],
            ], config)

            # ...添加智能体实现的其余部分

            # 提取答案，它很快会被赋值给状态
            answer = response.content

            return {
               "messages": response,
                # 在返回的状态中包含答案
               "answer": answer
            }


          # 最后，在编译图之前，我们定义 3 个状态组件
          builder = StateGraph(OverallState, input=InputState, output=OutputState)

          # 添加所有不同的节点和边并编译图
          builder.add_node("answer_node", answer_node)
          builder.add_edge(START, "answer_node")
          builder.add_edge("answer_node", END)
          graph = builder.compile()
          ```
          </Tab>
          <Tab value="TypeScript">
            <Callout type="warn">
              在我们致力于为 LangGraph TypeScript 添加 Zod 模式支持的同时，在定义"完整状态"时需要一种变通方法来忽略 TypeScript 错误。

              您可以在 TypeScript 实现代码片段的最后看到这一点。
            </Callout>
            ```typescript title="agent-js/sample_agent/agent.ts"
              import { Annotation } from "@langchain/langgraph";
              import { CopilotKitStateAnnotation } from "@copilotkit/sdk-js/langgraph";

              // 将状态分为 3 部分

              // 输入模式，用于您愿意从前端接受的输入
              const InputAnnotation = Annotation.Root({
                ...CopilotKitStateAnnotation.spec,
                question: Annotation<string>,
              });

              // 输出模式，用于您愿意传递给前端的输出
              const OutputAnnotation = Annotation.Root({
                ...CopilotKitStateAnnotation.spec,
                answer: Annotation<string>,
              });

              // 完整模式，包括输入、输出和内部状态（在我们的例子中是"resources"）
              export const AgentStateAnnotation = Annotation.Root({
                ...CopilotKitStateAnnotation.spec,
                ...OutputAnnotation.spec,
                ...InputAnnotation.spec,
                resources: Annotation<string[]>,
              });

              // 定义支持完整状态的类型状态
              export type AgentState = typeof AgentStateAnnotation.State;

              async function answerNode(state: AgentState, config: RunnableConfig) {
                const model = new ChatOpenAI()

                const systemMessage = new SystemMessage({
                  content: `你是一个有用的助手。回答问题：${state.question}。`,
                });

                const response = await modelWithTools.invoke(
                  [systemMessage, ...state.messages],
                  config
                );

                // ...添加智能体实现的其余部分
                // 提取答案，它很快会被赋值给状态
                const answer = response.content

                return {
                  messages: response,
                  // 在返回的状态中包含答案
                  answer,
                }
              }

              // 最后，在编译图之前，我们定义 3 个状态组件
              const workflow = new StateGraph({
                input: InputAnnotation,
                output: OutputAnnotation,
                // @ts-expect-error -- LangGraph 不期望具有"完整内部属性的模式"。
                stateSchema: AgentStateAnnotation,
              })
                .addNode("answer_node", answerNode) // 添加所有不同的节点和边并编译图
                .addEdge(START, "answer_node")
                .addEdge("answer_node", END)
              export const graph = workflow.compile()
            ```
          </Tab>
      </Tabs>
  </Step>
  <Step>
    ### 试试看！
    现在我们知道我们的智能体发出哪些状态属性，我们可以检查状态并预期发生以下情况：
    - 虽然我们能够提供一个 question，但我们不会从智能体那里收到它。如果我们在 UI 中使用它，我们需要记住 UI 是它的真实来源
    - Answer 一旦从智能体返回就会改变
    - UI 无法访问 resources。

    ```tsx
    import { useAgent } from "@copilotkit/react-core/v2"; // [!code highlight]

    const { agent } = useAgent({
      agentId: "sample_agent",
    });

    const answer = agent.state.answer as string;

    console.log(answer) // 您可以预期看到 "answer" 变化，而其他属性不会从智能体返回
    ```
  </Step>
</Steps>
