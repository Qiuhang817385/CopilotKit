---
title: 写入代理状态
icon: "lucide/ArrowRight"
description: 从您的应用程序写入代理的状态。
---
import { ImageZoom } from 'fumadocs-ui/components/image-zoom';
import { IframeSwitcher } from "@/components/content"
import RunAndConnect from "@/snippets/integrations/aws-strands/run-and-connect.mdx"

<IframeSwitcher
  id="shared-state-write-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/aws-strands/feature/shared_state?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/aws-strands/feature/shared_state?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout type="info">
  本示例演示了如何在 [CopilotKit 功能查看器](https://feature-viewer.copilotkit.ai/aws-strands/feature/shared_state) 中写入共享状态。
</Callout>

## 这是什么？

您可以轻松地从原生应用程序写入代理的状态，从而从您的 UI 更新代理的状态。

## 何时使用？

当您想向代理的状态提供用户输入或控制时，可以使用此功能。随着应用程序状态的变化，您可以更新代理状态以反映这些变化。

## 实现

<Steps>
  <Step>
    ### 运行并连接您的代理
    <RunAndConnect components={props.components} />
  </Step>
  <Step>
    ### 设置带状态的代理

    定义代理的状态模式。AWS Strands 会在整个执行过程中维护状态。

    ```python title="agent/main.py"
    import os
    from ag_ui_strands import StrandsAgent, StrandsAgentConfig, create_strands_app
    from strands import Agent
    from strands.models.openai import OpenAIModel

    model = OpenAIModel(
        client_args={"api_key": os.getenv("OPENAI_API_KEY", "")},
        model_id="gpt-4o",
    )

    # 创建 Strands 代理
    strands_agent = Agent(
        model=model,
        system_prompt="始终使用用户偏好的语言进行交流，如状态中定义的那样。不要使用其他语言进行交流。",
    )

    # 将状态注入提示
    def language_prompt(input_data, user_message: str) -> str:
        state_dict = getattr(input_data, "state", None)
        if isinstance(state_dict, dict) and "language" in state_dict:
            return f"当前语言: {state_dict['language']}\n\n用户请求: {user_message}"
        return user_message

    config = StrandsAgentConfig(state_context_builder=language_prompt)

    # 包装 AG-UI 集成
    agui_agent = StrandsAgent(
        agent=strands_agent,
        name="languageAgent",
        description="始终使用用户偏好的语言进行交流",
        config=config,
    )

    app = create_strands_app(agui_agent)
    ```
  </Step>

  <Step>
    ### 使用 `useAgent` Hook

    在代理连接并运行后，调用 `useAgent` hook，传入代理名称，并使用 `agent.setState` 更新代理状态。

    ```tsx title="ui/app/page.tsx"
    import { useAgent } from "@copilotkit/react-core/v2"; // [!code highlight]

    // 定义代理状态类型以匹配您的 Strands 代理
    type AgentState = {
      language: "english" | "spanish";
    };

    function YourMainContent() {
      // [!code highlight:5]
      const { agent } = useAgent({
        agentId: "languageAgent",
        // 可选地提供类型安全的初始状态
        initialState: { language: "spanish" }
      });

      const toggleLanguage = () => {
        agent.setState({ language: agent.state?.language === "english" ? "spanish" : "english" }); // [!code highlight]
      };

      return (
        // 为简洁起见省略样式
        <div>
          <h1>您的主要内容</h1>
          {/* [!code highlight:2] */}
          <p>语言: {agent.state?.language}</p>
          <button onClick={toggleLanguage}>切换语言</button>
        </div>
      );
    }
    ```
    <Callout type="info">
      `useAgent` 中的 `agent.setState` 函数将更新代理状态并在状态变化时触发重新渲染。
    </Callout>
  </Step>

  <Step>
    ### 试试看！

    您现在可以使用 `setState` 函数更新代理状态，并使用 `state` 读取它。尝试切换语言按钮并与您的代理交谈。您会看到语言变化以匹配代理的状态。

    <Callout type="info" title="重要">
      AWS Strands 中的共享状态是基于提示的。这意味着代理对共享状态的意识将通过您提供给代理的指令来增强。
    </Callout>
  </Step>
</Steps>
