---
title: 快速开始
description: 在 10 分钟内将您的 Pydantic AI 智能体转变为智能体原生应用。
icon: "lucide/Play"
---

import {
  TailoredContent,
  TailoredContentOption,
} from "@/components/react/tailored-content.tsx";
import { CoAgentsEnterpriseCTA } from "@/components/react/coagents/coagents-enterprise-cta.tsx";
import { CoAgentsDiagram } from "@/components/react/coagents/coagents-diagram.tsx";
import { FaPython, FaJs, FaCloud } from "react-icons/fa";
import SelfHostingCopilotRuntimeCreateEndpoint from "@/snippets/_cn/self-hosting-copilot-runtime-create-endpoint.mdx";
import CopilotKitCloudCopilotKitProvider from "@/snippets/_cn/copilot-cloud-configure-copilotkit-provider.mdx";
import { Accordions, Accordion } from "fumadocs-ui/components/accordion";
import FindYourCopilotRuntime from "@/snippets/_cn/find-your-copilot-runtime.mdx";
import CloudCopilotKitProvider from "@/snippets/coagents_cn/cloud-configure-copilotkit-provider.mdx";
import SelfHostingCopilotRuntimeConfigureCopilotKitProvider from "@/snippets/coagents_cn/self-host-configure-copilotkit-provider.mdx";
import SelfHostingCopilotRuntimeLangGraphEndpoint from "@/snippets/_cn/self-hosting-copilot-runtime-langgraph-endpoint.mdx";
import SelfHostingCopilotRuntimeStarter from "@/snippets/_cn/self-hosting-copilot-runtime-starter.mdx";
import SelfHostingRemoteEndpoints from "@/snippets/_cn/self-hosting-remote-endpoints.mdx";
import { PydanticAIIcon } from "@/lib/icons/custom-icons";
import {
  UserIcon,
  PaintbrushIcon,
  WrenchIcon,
  RepeatIcon,
  ServerIcon,
} from "lucide-react";
import CopilotUI from "@/snippets/_cn/copilot-ui.mdx";

<video
  src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/chat-example.mp4"
  className="rounded-lg shadow-xl"
  loop
  playsInline
  controls
  autoPlay
  muted
/>

## 前提条件

在开始之前，您需要准备以下内容：

- [**OpenAI API 密钥**](https://platform.openai.com/api-keys)

## 开始使用

<Steps>
    <TailoredContent
        className="step"
        id="path"
        header={
            <div>
                <p className="text-xl font-semibold">您是否已经拥有 Pydantic AI 智能体？</p>
                <p className="text-base">
                    您可以快速创建一个新项目，或将现有的智能体集成到 CopilotKit 中。
                </p>
            </div>
        }
    >
        <TailoredContentOption
            id="create"
            title="从零开始"
            description="我还没有智能体，但想快速开始使用。"
            icon={<img src="https://cdn.copilotkit.ai/docs/copilotkit/images/copilotkit-logo.svg" alt="CopilotKit Logo" width={20} height={20} />}
        >
            <Step>
                ### 运行我们的 CLI

                ```bash
                npx copilotkit@latest create -f pydantic-ai
                ```
            </Step>
            <Step>
                ### 安装依赖
                ```package-install
                ```
                <Callout type="info">
                    这也会安装您智能体的 Python 依赖！如果遇到问题，请尝试运行 `npm run install:agent`。
                </Callout>
            </Step>
            <Step>
                ### 配置 OpenAI API 密钥
                您的智能体需要一个 LLM 来进行对话。本示例使用 OpenAI，但您可以使用任何您想要的 LLM 提供商。

                ```bash
                export OPENAI_API_KEY=your_openai_api_key
                ```

                或者在您智能体的 `.env` 文件中设置。

                ```plaintext title="agent/.env"
                OPENAI_API_KEY=your_openai_api_key
                ```
            </Step>
            <Step>
                ### 运行您的智能体
                现在您可以在同一个终端中同时运行智能体和 UI！

                ```bash
                npm run dev
                ```
            </Step>
        </TailoredContentOption>
        <TailoredContentOption
            id="exiting-agent"
            title="使用现有智能体"
            description="我已经有一个 Pydantic AI 智能体，想将其与 CopilotKit 一起使用。"
            icon={<PydanticAIIcon className="w-4 h-4 text-bold" />}
        >
            <Step>
                ### 添加必要的依赖
                Pydantic AI 的集成依赖于 [AG-UI](https://docs.ag-ui.com/)，因此
                您需要将其安装到您的项目中。

                我们的示例还将使用 uvicorn 来运行智能体。

                ```sh
                pip install 'pydantic-ai-slim[ag-ui]' uvicorn
                ```
            </Step>
            <Step>
                ### 将您的智能体暴露为 AG-UI ASGI 应用程序

                ```python title="agent.py"
                from pydantic_ai import Agent

                agent = Agent('openai:gpt-4.1', instructions='Be fun!')
                app = agent.to_ag_ui()

                # 如果您希望在调用时运行服务器，可以执行以下操作：
                if __name__ == "__main__":
                    import uvicorn
                    uvicorn.run(app, host="localhost", port=8000)

                ```
            </Step>
            <Step>
                ### 创建您的前端
                CopilotKit 可以在任何运行 React 的地方运行。因此，您可以使用 Next.js、
                Vite 或任何其他 React 环境。

                对于本示例，我们将创建一个新的 Next.js 应用。

                ```bash
                npx create-next-app@latest
                ```
            </Step>
            <Step>
                ### 安装 CopilotKit
                首先，将最新的 CopilotKit 包安装到您的前端。
                ```package-install
                npm install @copilotkit/react-ui @copilotkit/react-core @copilotkit/runtime
                ```
            </Step>
            <Step>
                ### 设置 Copilot Runtime
                CopilotKit 的运行时负责智能体与前端之间的安全通信。随着您的
                应用程序的进展，您将在这里设置线程或身份验证。

                现在，让我们从一个最小化的设置开始。

                ```tsx title="app/api/copilotkit/route.ts"
                import {
                  CopilotRuntime,
                  ExperimentalEmptyAdapter,
                  copilotRuntimeNextJSAppRouterEndpoint,
                } from "@copilotkit/runtime";
                import { HttpAgent } from "@ag-ui/client";
                import { NextRequest } from "next/server";

                // 1. 您可以在这里使用任何服务适配器来实现多智能体支持。我们使用
                //    空适配器，因为我们只使用一个智能体。
                const serviceAdapter = new ExperimentalEmptyAdapter();

                // 2. 创建 CopilotRuntime 实例并利用 PydanticAI AG-UI
                //    集成来设置连接。
                const runtime = new CopilotRuntime({
                  agents: {
                    // 我们的 AG-UI 端点 URL
                    "my_agent": new HttpAgent({ url: "http://localhost:8000/" }),
                  }
                });

                // 3. 构建一个处理 CopilotKit 运行时请求的 Next.js API 路由。
                export const POST = async (req: NextRequest) => {
                  const { handleRequest } = copilotRuntimeNextJSAppRouterEndpoint({
                    runtime,
                    serviceAdapter,
                    endpoint: "/api/copilotkit",
                  });

                  return handleRequest(req);
                };
               ```
            </Step>
            <Step>
                ### 设置 CopilotKit 提供程序
                CopilotKit 提供程序组件负责管理与当前智能体的会话。通常，
                用户会将整个应用程序包裹在 CopilotKit 提供程序中。

                ```tsx title="app/layout.tsx"
                import "./globals.css";
                import { ReactNode } from "react";
                import { CopilotKit } from "@copilotkit/react-core";

                export default function RootLayout({ children }: { children: ReactNode }) {
                  return (
                    <html lang="en">
                      <body>
                        {/* 这指向我们在上一步设置的运行时 */}
                        {/* [!code highlight:3] */}
                        <CopilotKit runtimeUrl="/api/copilotkit" agent="my_agent">
                          {children}
                        </CopilotKit>
                      </body>
                    </html>
                  );
                }
                ```
            </Step>
            <Step>
                ### 设置 Copilot UI
                最后一步是使用 CopilotKit 的 UI 组件来渲染与智能体的聊天交互。在大多数情况下，
                这是与核心页面组件一起完成的，例如在您的 `page.tsx` 文件中。

                ```tsx title="page.tsx"
                // [!code highlight:2]
                import "@copilotkit/react-ui/v2/styles.css";
                import { CopilotSidebar } from "@copilotkit/react-core/v2";

                export function YourApp() {
                return (
                    <main>
                    <h1>您的主内容</h1>
                    {/* [!code highlight:6] */}
                    <CopilotSidebar
                        labels={{
                            modalHeaderTitle: "弹出助手",
                            welcomeMessageText: "嗨！我已连接到智能体。我能帮您什么？",
                        }}
                    />
                    </main>
                );
                }
                ```

                <Callout type="info">
                    寻找其他聊天组件选项？查看我们的 [Agentic Chat UI](/pydantic-ai/prebuilt-components) 指南。
                </Callout>
            </Step>
        </TailoredContentOption>
    </TailoredContent>
    <Step>
        ### 🎉 与您的智能体对话！

        恭喜！您已成功将 Pydantic AI 智能体聊天机器人集成到您的应用程序中。首先，尝试向您的智能体问几个问题。

        ```
        能给我讲个笑话吗？
        ```

        ```
        你能帮我理解 AI 吗？
        ```

        ```
        你觉得 React 怎么样？
        ```

        <video src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/chat-example.mp4" className="rounded-lg shadow-xl" loop playsInline controls autoPlay muted />

        <Accordions className="mb-4">
            <Accordion title="遇到问题？">
                - 尝试将主机更改为 `0.0.0.0` 或 `127.0.0.1` 而不是 `localhost`。
            </Accordion>
        </Accordions>
    </Step>

</Steps>

---

## 下一步是什么？

您现在已经在 CopilotKit 中运行了一个 Pydantic AI 智能体！现在您可以开始探索 CopilotKit
帮助您构建强大的智能体原生应用程序的各种方式。

<Cards>
  <Card
    title="实现人机协同"
    description="让您的用户和智能体协作完成任务。"
    href="/pydantic-ai/human-in-the-loop"
    icon={<UserIcon />}
  />
  <Card
    title="利用共享状态"
    description="了解如何将智能体的状态与 UI 的状态同步，反之亦然。"
    href="/pydantic-ai/shared-state"
    icon={<RepeatIcon />}
  />
  <Card
    title="添加生成式 UI"
    description="在 UI 中渲染智能体的进度和输出。"
    href="/pydantic-ai/generative-ui/tool-rendering"
    icon={<PaintbrushIcon />}
  />
  <Card
    title="设置前端操作"
    description="让您的智能体能够调用前端工具，直接更新您的应用程序。"
    href="/pydantic-ai/frontend-tools"
    icon={<WrenchIcon />}
  />
</Cards>
