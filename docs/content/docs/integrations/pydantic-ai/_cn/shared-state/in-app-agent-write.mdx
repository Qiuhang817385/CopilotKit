---
title: 写入 Agent 状态
icon: "lucide/ArrowRight"
description: 从您的应用程序写入 Agent 的状态。
---
import RunAndConnect from "@/snippets/integrations/pydantic-ai/run-and-connect.mdx"
import { IframeSwitcher } from "@/components/content"

<IframeSwitcher
  id="shared-state-write-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/pydantic-ai/feature/shared_state?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/pydantic-ai/feature/shared_state?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout type="info">
  此示例演示了 [CopilotKit 功能查看器](https://feature-viewer.copilotkit.ai/pydantic-ai/feature/shared_state) 中的写入共享状态。
</Callout>

## 这是什么？

本指南向您展示如何从应用程序写入 Agent 的状态。

## 何时使用此功能？

当 Agent 调用工具时，您可以使用此功能向用户提供有关 Agent 正在做什么的反馈。CopilotKit 允许您完全自定义这些工具在聊天中的渲染方式。

## 实现

<Steps>
  <Step>
    ### 运行并连接您的 Agent
    <RunAndConnect components={props.components} />
  </Step>
  <Step>
    ### 定义 Agent 状态

    使用有状态结构创建您的 Pydantic AI Agent。以下是一个跟踪语言的完整示例：

    ```python title="agent.py"
    from textwrap import dedent

    from pydantic import BaseModel
    from pydantic_ai import Agent, RunContext
    from pydantic_ai.ag_ui import StateDeps


    class AgentState(BaseModel):
        """Agent 的状态。"""
        language: str = "english"


    agent = Agent("openai:gpt-5.2-mini", deps_type=StateDeps[AgentState])


    @agent.instructions()
    async def language_instructions(ctx: RunContext[StateDeps[AgentState]]) -> str:
        """语言跟踪 Agent 的指令。

        Args:
            ctx: 包含语言状态信息的运行上下文。

        Returns:
            语言跟踪 Agent 的指令字符串。
        """
        return dedent(
            f"""
            您是一个用于跟踪语言的有用助手。

            重要：
            - 语言始终使用小写
            - 始终使用当前语言回复：{ctx.deps.state.language}
            """
        )


    app = agent.to_ag_ui(deps=StateDeps(AgentState()))

    if __name__ == "__main__":
        import uvicorn
        uvicorn.run(app, host="0.0.0.0", port=8000)
    ```

  </Step>
  <Step>
    ### 从 `useAgent` Hook 调用 `agent.setState`
    `useAgent` 返回一个带有 `setState` 方法的 `agent` 对象，您可以使用该方法更新 Agent 状态。调用此方法将更新 Agent 状态并触发任何依赖于 Agent 状态的内容重新渲染。

    ```tsx title="ui/app/page.tsx"
    import { useAgent } from "@copilotkit/react-core/v2"; // [!code highlight]

    // 定义 Agent 状态类型，应与您的 Agent 实际状态匹配
    type AgentState = {
      language: "english" | "spanish";
    }

    // 伪 React 组件中的示例用法
    function YourMainContent() {
      const { agent } = useAgent({ // [!code highlight]
        agentId: "my_agent",
        initialState: { language: "english" }  // 可选地提供初始状态
      });

      // ...

      const toggleLanguage = () => {
        agent.setState({ language: agent.state?.language === "english" ? "spanish" : "english" }); // [!code highlight]
      };

      // ...

      return (
        // 为简洁起见，省略了样式
        <div>
          <h1>您的主内容</h1>
          {/* [!code highlight:2] */}
          <p>语言：{agent.state?.language}</p>
          <button onClick={toggleLanguage}>切换语言</button>
        </div>
      );
    }
    ```

    <Callout type="warn" title="重要">
      `name` 参数必须与您在 CopilotRuntime 配置中定义的 Agent 名称完全匹配（例如快速入门中的 `my_agent`）。
    </Callout>

  </Step>
  <Step>
    ### 试一试！
    现在您可以使用 `agent.setState` 更新 Agent 状态，并使用 `agent.state` 读取它。尝试切换语言按钮并与您的 Agent 交谈。您会看到语言根据 Agent 的状态而变化。
  </Step>
</Steps>

## 高级用法

### 使用有关更改内容的提示重新运行 Agent

新的 Agent 状态将在下次 Agent 运行时使用。
如果您想手动重新运行它，请使用 `copilotkit.runAgent()`。

Agent 将使用最新的更新状态重新运行。您还可以在重新运行之前添加提示消息。

```tsx title="ui/app/page.tsx"
import { useAgent, useCopilotKit } from "@copilotkit/react-core/v2";

// ...

function YourMainContent() {
  const { agent } = useAgent({
    agentId: "my_agent",
  });
  const { copilotkit } = useCopilotKit(); // [!code highlight]

  // 设置为在应用程序中发生某些事件时调用
  const toggleLanguage = async () => {
    const newLanguage = agent.state?.language === "english" ? "spanish" : "english";
    agent.setState({ language: newLanguage });

    // 添加提示消息并重新运行 Agent
    // [!code highlight:7]
    agent.addMessage({
      id: crypto.randomUUID(),
      role: "user",
      content: `语言已更新为 ${newLanguage}`,
    });
    await copilotkit.runAgent({ agent });
  };

  return (
    // ...
  );
}
```

### 中间流式传输和渲染 Agent 状态

默认情况下，Pydantic AI Agent 状态仅在 Pydantic AI Agent 节点转换_之间_更新——这意味着状态更新将是不连续且延迟的。

{/* 您可能希望将 Agent 状态渲染为**连续**更新。

请参阅**[发射中间状态](/pydantic-ai/shared-state/predictive-state-updates)。**  */}
