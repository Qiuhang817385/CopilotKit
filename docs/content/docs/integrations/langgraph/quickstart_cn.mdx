---
title: 快速开始
description: 在 10 分钟内将您的 LangGraph 转变为 Agent-Native 应用程序。
icon: "lucide/Play"
hideTOC: true
---

import {
  TailoredContent,
  TailoredContentOption,
} from "@/components/react/tailored-content.tsx";
import { CoAgentsEnterpriseCTA } from "@/components/react/coagents/coagents-enterprise-cta.tsx";
import { CoAgentsDiagram } from "@/components/react/coagents/coagents-diagram.tsx";
import { FaPython, FaJs, FaCloud } from "react-icons/fa";
import SelfHostingCopilotRuntimeCreateEndpoint from "@/snippets/_cn/self-hosting-copilot-runtime-create-endpoint.mdx";
import CopilotCloudConfigureRemoteEndpointLangGraph from "@/snippets/_cn/copilot-cloud-configure-remote-endpoint-langgraph.mdx";
import CopilotKitCloudCopilotKitProvider from "@/snippets/_cn/copilot-cloud-configure-copilotkit-provider.mdx";
import LangGraphPlatformDeploymentTabs from "@/snippets/_cn/langgraph-platform-deployment-tabs.mdx";
import { Accordions, Accordion } from "fumadocs-ui/components/accordion";
import FindYourCopilotRuntime from "@/snippets/_cn/find-your-copilot-runtime.mdx";
import ConnectCopilotUI from "@/snippets/_cn/copilot-ui.mdx";
import CloudCopilotKitProvider from "@/snippets/coagents_cn/cloud-configure-copilotkit-provider.mdx";
import SelfHostingCopilotRuntimeConfigureCopilotKitProvider from "@/snippets/coagents_cn/self-host-configure-copilotkit-provider.mdx";
import SelfHostingCopilotRuntimeLangGraphEndpoint from "@/snippets/_cn/self-hosting-copilot-runtime-langgraph-endpoint.mdx";
import SelfHostingCopilotRuntimeStarter from "@/snippets/_cn/self-hosting-copilot-runtime-starter.mdx";
import SelfHostingRemoteEndpoints from "@/snippets/_cn/self-hosting-remote-endpoints.mdx";
import {
  UserIcon,
  PaintbrushIcon,
  WrenchIcon,
  RepeatIcon,
  ServerIcon,
} from "lucide-react";
import { SiLangchain } from "react-icons/si";
import CopilotUI from "@/snippets/_cn/copilot-ui.mdx";
import { PathIcon } from "lucide-react";
import { SquareTerminal, SquareChartGantt } from "lucide-react";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

<video
  src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/chat-example.mp4"
  className="rounded-lg shadow-xl"
  loop
  playsInline
  controls
  autoPlay
  muted
/>

## 前置条件

在开始之前，您需要准备以下内容：

- OpenAI API 密钥
- Node.js 20+
- 您喜欢的包管理器
- （可选）LangSmith API 密钥 - 仅在使用现有 LangGraph Agent 时需要

## 开始使用

<Steps>
    <TailoredContent
        className="step"
        id="agent"
        header={
            <div>
                <p className="text-xl font-semibold">选择您的起点</p>
                <p className="text-base">
                    您可以从我们的入门模板开始，或将 CopilotKit 集成到现有的 LangGraph Agent 中。
                </p>
            </div>
        }
    >
        <TailoredContentOption
            id="starter"
            title="从零开始"
            description="使用我们现成的入门应用程序快速开始。"
        >
            <Step>
                ### 运行我们的 CLI

                首先，我们将使用 CLI 创建一个新项目。在 Python 和 JavaScript 之间选择：

                <Tabs groupId="language_langgraph_agent" items={['Python', 'JavaScript']} persist>
                    <Tab value="Python">
                        ```bash
                        npx copilotkit@latest create -f langgraph-py
                        ```
                    </Tab>
                    <Tab value="JavaScript">
                        ```bash
                        npx copilotkit@latest create -f langgraph-js
                        ```
                    </Tab>
                </Tabs>
            </Step>
            <Step>
                ### 安装依赖

                ```npm
                npm install
                ```
            </Step>
            <Step>
                ### 配置环境

                在 Agent 目录中创建 `.env` 文件并添加您的 OpenAI API 密钥：

                ```plaintext title=".env"
                OPENAI_API_KEY=your_openai_api_key
                ```

                <Callout type="info" title="其他模型呢？">
                  入门模板默认配置使用 OpenAI 的 GPT-4o，但您可以修改它以使用 LangGraph 支持的任何语言模型。
                </Callout>
            </Step>
            <Step>
                ### 启动开发服务器

                <Tabs groupId="package-manager" items={['npm', 'pnpm', 'yarn', 'bun']}>
                    <Tab value="npm">
                        ```bash
                        npm run dev
                        ```
                    </Tab>
                    <Tab value="pnpm">
                        ```bash
                        pnpm dev
                        ```
                    </Tab>
                    <Tab value="yarn">
                        ```bash
                        yarn dev
                        ```
                    </Tab>
                    <Tab value="bun">
                        ```bash
                        bun dev
                        ```
                    </Tab>
                </Tabs>

                这将同时启动 UI 和 Agent 服务器。
            </Step>
        </TailoredContentOption>
        <TailoredContentOption
            id="bring-your-own"
            title="使用现有 Agent"
            description="我已经有一个 LangGraph Agent，想要添加 CopilotKit。"
        >
          <Step>
              ### 初始化 Agent 项目

              如果您还没有设置 Python 项目，请使用 `uv` 创建一个：

              ```bash
              uv init my-agent
              cd my-agent
              ```
          </Step>
          <Step>
              ### 安装 LangGraph 和 AG-UI

              将 LangGraph 和所需的 AG-UI 包添加到您的项目：

              ```bash
              uv add langgraph copilotkit langchain-openai langchain-core
              ```
          </Step>
          <Step>
            ### 通过 AG-UI 暴露您的 Agent

            如果您已经编写了 LangGraph Agent，只需参考以下代码。在此步骤中
            我们创建一个简单的 LangGraph Agent 用于演示。
              <Tabs groupId="deployment_method" items={['LangSmith', 'FastAPI']}>
                <Tab value="LangSmith">
                  首先，我们将创建一个简单的 LangGraph Agent：

                  ```python title="main.py"
                  from langchain_core.messages import SystemMessage
                  from langchain_openai import ChatOpenAI
                  from langgraph.graph import END, START, MessagesState, StateGraph

                  async def mock_llm(state: MessagesState):
                    model = ChatOpenAI(model="gpt-4.1-mini")
                    system_message = SystemMessage(content="You are a helpful assistant.")
                    response = await model.ainvoke(
                      [
                        system_message,
                        *state["messages"],
                      ]
                    )
                    return {"messages": response}

                  graph = StateGraph(MessagesState)
                  graph.add_node(mock_llm)
                  graph.add_edge(START, "mock_llm")
                  graph.add_edge("mock_llm", END)
                  graph = graph.compile()
                  ```

                  然后要通过 LangSmith 测试和部署，我们还需要一个 `langgraph.json`

                  ```sh
                  touch langgraph.json
                  ```

                  ```json title="langgraph.json"
                  {
                    "python_version": "3.12",
                    "dockerfile_lines": [],
                    "dependencies": ["."],
                    "package_manager": "uv",
                    "graphs": {
                      "sample_agent": "./main.py:graph"
                    },
                    "env": ".env"
                  }
                  ```
                </Tab>
                <Tab value="FastAPI">
                  首先，将 `ag-ui-langgraph` 包添加到您的项目：

                  ```bash
                  uv add ag-ui-langgraph fastapi uvicorn copilotkit
                  ```

                  然后创建一个简单的 LangGraph Agent，添加 FastAPI 应用，并将我们的 Agent 作为 AG-UI 端点附加。

                  ```python title="main.py"
                  import os

                  # [!code highlight:2]
                  from ag_ui_langgraph import add_langgraph_fastapi_endpoint
                  from copilotkit import LangGraphAGUIAgent
                  from fastapi import FastAPI
                  from langgraph.graph import END, START, MessagesState, StateGraph
                  from langchain_core.messages import SystemMessage
                  from langchain_openai import ChatOpenAI

                  async def mock_llm(state: MessagesState):
                    model = ChatOpenAI(model="gpt-4.1-mini")
                    system_message = SystemMessage(content="You are a helpful assistant.")
                    response = await model.ainvoke(
                      [
                        system_message,
                        *state["messages"],
                      ]
                    )
                    return {"messages": response}


                  graph = StateGraph(MessagesState)
                  graph.add_node(mock_llm)
                  graph.add_edge(START, "mock_llm")
                  graph.add_edge("mock_llm", END)
                  graph = graph.compile()

                  app = FastAPI()

                  # [!code highlight:9]
                  add_langgraph_fastapi_endpoint(
                    app=app,
                    agent=LangGraphAGUIAgent(
                      name="sample_agent",
                      description="An example agent to use as a starting point for your own agent.",
                      graph=graph,
                    ),
                    path="/",
                  )

                  def main():
                    """Run the uvicorn server."""
                    uvicorn.run(
                      "main:app",
                      host="0.0.0.0",
                      port="8123",
                      reload=True,
                    )

                  if __name__ == "__main__":
                    main()
                  ```
                </Tab>
              </Tabs>

              <Callout type="info" title="什么是 AG-UI？">
                AG-UI 是一个用于前端-Agent 通信的开放协议。
              </Callout>
          </Step>
          <Step>
              ### 配置环境

              在 Agent 目录中创建 `.env` 文件并添加您的 OpenAI API 密钥：

              ```plaintext title=".env"
              OPENAI_API_KEY=your_openai_api_key
              ```

              <Callout type="info" title="其他模型呢？">
                入门模板默认配置使用 OpenAI 的 GPT-4o，但您可以修改它以使用 LangGraph 支持的任何语言模型。
              </Callout>
          </Step>
          <Step>
              ### 创建前端

              CopilotKit 可与任何基于 React 的前端配合使用。在此示例中，我们将使用 Next.js。

              ```bash
              npx create-next-app@latest frontend
              cd frontend
              ```
          </Step>
          <Step>
              ### 安装 CopilotKit 包

              ```npm
              npm install @copilotkit/react-ui @copilotkit/react-core @copilotkit/runtime
              ```
          </Step>
          <Step>
              ### 设置 Copilot Runtime

              创建一个 API 路由以将 CopilotKit 连接到您的 LangGraph Agent：

              ```sh
              mkdir -p app/api/copilotkit && touch app/api/copilotkit/route.ts
              ```

              <Tabs groupId="deployment_method" items={['LangSmith', 'FastAPI']}>
                <Tab value="LangSmith">
                  ```tsx title="app/api/copilotkit/route.ts"
                  import {
                    CopilotRuntime,
                    ExperimentalEmptyAdapter,
                    copilotRuntimeNextJSAppRouterEndpoint,
                  } from "@copilotkit/runtime";
                  // [!code highlight]
                  import { LangGraphAgent } from "@copilotkit/runtime/langgraph";
                  import { NextRequest } from "next/server";

                  const serviceAdapter = new ExperimentalEmptyAdapter();

                  const runtime = new CopilotRuntime({
                    agents: {
                    // [!code highlight:5]
                      sample_agent: new LangGraphAgent({
                        deploymentUrl:  process.env.LANGGRAPH_DEPLOYMENT_URL || "http://localhost:8123",
                        graphId: "sample_agent",
                        langsmithApiKey: process.env.LANGSMITH_API_KEY || "",
                      }),
                    }
                  });

                  export const POST = async (req: NextRequest) => {
                    const { handleRequest } = copilotRuntimeNextJSAppRouterEndpoint({
                      runtime,
                      serviceAdapter,
                      endpoint: "/api/copilotkit",
                    });

                    return handleRequest(req);
                  };
                ```
              </Tab>
              <Tab value="FastAPI">
                ```tsx title="app/api/copilotkit/route.ts"
                import {
                  CopilotRuntime,
                  ExperimentalEmptyAdapter,
                  copilotRuntimeNextJSAppRouterEndpoint,
                } from "@copilotkit/runtime";
                // [!code highlight]
                import { LangGraphHttpAgent } from "@copilotkit/runtime/langgraph";
                import { NextRequest } from "next/server";

                const serviceAdapter = new ExperimentalEmptyAdapter();

                const runtime = new CopilotRuntime({
                  agents: {
                    // [!code highlight:3]
                    sample_agent: new LangGraphHttpAgent({
                      url:  process.env.LANGGRAPH_DEPLOYMENT_URL || "http://localhost:8123",
                    }),
                  }
                });

                export const POST = async (req: NextRequest) => {
                  const { handleRequest } = copilotRuntimeNextJSAppRouterEndpoint({
                    runtime,
                    serviceAdapter,
                    endpoint: "/api/copilotkit",
                  });

                  return handleRequest(req);
                };
              ```
              </Tab>
            </Tabs>
          </Step>
          <Step>
              ### 配置 CopilotKit Provider

              用 CopilotKit Provider 包装您的应用程序：

              ```tsx title="app/layout.tsx"
              // [!code highlight:2]
              import { CopilotKit } from "@copilotkit/react-core";
              import "@copilotkit/react-ui/v2/styles.css";

              // ...

              export default function RootLayout({ children }: {children: React.ReactNode}) {
                return (
                  <html lang="en">
                    <body>
                      {/* [!code highlight:3] */}
                      <CopilotKit runtimeUrl="/api/copilotkit" agent="sample_agent">
                        {children}
                      </CopilotKit>
                    </body>
                  </html>
                );
              }
              ```
          </Step>
          <Step>
            ### 添加聊天界面

            将 CopilotSidebar 组件添加到您的页面：

            ```tsx title="app/page.tsx"
            import { CopilotSidebar } from "@copilotkit/react-core/v2"; // [!code highlight:1]

            export default function Page() {
              return (
                <main>
                  <h1>Your App</h1>
                  {/* [!code highlight:1] */}
                  <CopilotSidebar />
                </main>
              );
            }
            ```
          </Step>
          <Step>
              ### 启动 Agent
              在 Agent 目录中，启动 Agent 服务器：

              <Tabs groupId="deployment_method" items={['LangSmith', 'FastAPI']}>
                <Tab value="LangSmith">
                  ```bash
                  cd ..
                  npx @langchain/langgraph-cli dev --port 8123 --no-browser
                  ```
                </Tab>
                <Tab value="FastAPI">
                  ```bash
                  cd ..
                  uv run main.py
                  ```
                </Tab>
              </Tabs>

              您的 Agent 将在 `http://localhost:8123` 可用。
          </Step>
          <Step>
              ### 启动 UI

              在单独的终端中，导航到前端目录并启动开发服务器：

              <Tabs groupId="package-manager" items={['npm', 'pnpm', 'yarn', 'bun']}>
                  <Tab value="npm">
                      ```bash
                      cd frontend
                      npm run dev
                      ```
                  </Tab>
                  <Tab value="pnpm">
                      ```bash
                      cd frontend
                      pnpm dev
                      ```
                  </Tab>
                  <Tab value="yarn">
                      ```bash
                      cd frontend
                      yarn dev
                      ```
                  </Tab>
                  <Tab value="bun">
                      ```bash
                      cd frontend
                      bun dev
                      ```
                  </Tab>
              </Tabs>
          </Step>
        </TailoredContentOption>
    </TailoredContent>
    <Step>
        ### 🎉 开始聊天！

        您的 AI Agent 现在可以使用了！试着问它一些问题：

        ```
        能给我讲个笑话吗？
        ```

        ```
        你能帮我理解 AI 吗？
        ```

        ```
        你觉得 React 怎么样？
        ```

        <Accordions className="mb-4">
            <Accordion title="故障排除">
                - 如果遇到连接问题，尝试使用 `0.0.0.0` 或 `127.0.0.1` 代替 `localhost`
                - 确保 Agent 文件夹包含 `langgraph.json` 文件
                - 在 `langgraph.json` 文件中，引用 `.env` 文件的路径
                - 检查您的 OpenAI API 密钥是否已在 `.env` 文件中正确设置
                - 如果使用现有 Agent，请确保也配置了 LangSmith API 密钥
                - 确保在运行 `langgraph dev` 命令时位于 `langgraph.json` 文件所在的同一文件夹中
            </Accordion>
        </Accordions>

    </Step>
</Steps>

## 接下来做什么？

现在您已经设置了基本 Agent，探索这些高级功能：

<Cards>
  <Card
    title="实现人机协同"
    description="允许您的用户和 Agent 协同完成任务。"
    href="/langgraph/human-in-the-loop"
    icon={<UserIcon />}
  />
  <Card
    title="利用共享状态"
    description="了解如何将 Agent 的状态与 UI 的状态同步，反之亦然。"
    href="/langgraph/shared-state"
    icon={<RepeatIcon />}
  />
  <Card
    title="添加生成式 UI"
    description="在 UI 中渲染 Agent 的进度和输出。"
    href="/langgraph/generative-ui/tool-rendering"
    icon={<PaintbrushIcon />}
  />
  <Card
    title="设置前端操作"
    description="赋予 Agent 调用前端工具的能力，直接更新您的应用程序。"
    href="/langgraph/frontend-tools"
    icon={<WrenchIcon />}
  />
</Cards>
