---
title: 中断式
icon: "lucide/CirclePause"
description: 学习如何使用中断式流程实现人机协同（HITL）。
---
import InstallSDKSnippet from "@/snippets/_cn/install-sdk.mdx"
import {
  TailoredContent,
  TailoredContentOption,
} from "@/components/react/tailored-content.tsx";
import RunAndConnectAgentSnippet from "@/snippets/coagents_cn/run-and-connect-agent.mdx"
import { MessageCircle, PanelsTopLeft } from "lucide-react";
import { IframeSwitcher } from "@/components/content"
import RunAndConnect from "@/snippets/integrations/langgraph_cn/run-and-connect.mdx"
import { Tabs, Tab } from "fumadocs-ui/components/tabs";

<IframeSwitcher
  id="human-in-the-loop-example"
  exampleUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/human_in_the_loop?sidebar=false&chatDefaultOpen=false"
  codeUrl="https://feature-viewer.copilotkit.ai/langgraph/feature/human_in_the_loop?view=code&sidebar=false&codeLayout=tabs"
  exampleLabel="演示"
  codeLabel="代码"
  height="700px"
/>

<Callout type="info">
  此示例在 [CopilotKit 功能查看器](https://feature-viewer.copilotkit.ai/langgraph/feature/human_in_the_loop) 中演示了基于中断的人机协同（HITL）。
</Callout>

## 这是什么？

[LangGraph 的中断流程](https://docs.langchain.com/oss/python/langgraph/interrupts) 提供了一种直观的方式来实现人机协同工作流。

本指南将展示如何使用 `interrupt` 以及如何将其与 CopilotKit 集成。

## 何时使用？

人机协同是实现可用于生产的复杂工作流的强大方式。通过让人参与循环，
您可以确保 Agent 始终做出正确的决策，并最终朝着正确的方向前进。

中断式流程是实现人机协同的非常直观的方式。节点可以在执行过程中被中断以允许用户输入，而不是在执行之前或之后等待用户输入。
节点。代价是 Agent 不知道交互过程，但 [CopilotKit 的 SDK 提供了辅助函数来缓解这个问题](#让-agent-感知中断)。

## 实现

<Steps>
<Step>
  ### 运行并连接您的 Agent
  <RunAndConnect components={props.components} />
</Step>
<Step>
  ### 安装 CopilotKit SDK
  <InstallSDKSnippet components={props.components} />
</Step>
<Step>
### 设置 Agent 状态
我们将让 Agent 要求我们为它们命名，因此我们需要一个状态属性来存储名称。

<Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
    <Tab value="Python">
        ```python title="agent.py"
        # ...
        from copilotkit import CopilotKitState # 扩展 MessagesState
        # ...

        # 这是 Agent 的状态。
        # 它继承了 CopilotKit 的 CopilotKitState 属性。
        class AgentState(CopilotKitState):
            agent_name: str
        ```
    </Tab>
    <Tab value="TypeScript">
        ```typescript title="agent-js/src/agent.ts"
        // ...
        import { Annotation } from "@langchain/langgraph";
        import { CopilotKitStateAnnotation } from "@copilotkit/sdk-js/langgraph";
        // ...

        // 这是 Agent 的状态。
        // 它继承了 CopilotKit 的 CopilotKitState 属性。
        export const AgentStateAnnotation = Annotation.Root({
          agentName: Annotation<string>,
          ...CopilotKitStateAnnotation.spec,
        });
        export type AgentState = typeof AgentStateAnnotation.State;
        ```
    </Tab>
</Tabs>

</Step>
<Step>
    ### 在 LangGraph Agent 中调用 `interrupt`
    现在我们可以在 LangGraph Agent 中调用 `interrupt`。

    <Callout type="info">
        默认情况下，Agent 不会感知 `interrupt` 交互。

        如果您想要此行为，请参阅下面的[相关部分](#让-agent-感知中断)。
    </Callout>

    <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
        <Tab value="Python">
            ```python title="agent.py"
            from langgraph.types import interrupt # [!code highlight]
            from langchain_core.messages import SystemMessage
            from langchain_openai import ChatOpenAI
            from copilotkit import CopilotKitState

            # 添加上一步的 agent 状态定义
            class AgentState(CopilotKitState):
                agent_name: str

            def chat_node(state: AgentState, config: RunnableConfig):
                if not state.get("agent_name"):
                    # 中断并等待用户回复名称
                    state["agent_name"] = interrupt("在我们开始之前，您想叫我什么名字？") # [!code highlight]

                # 告诉 Agent 它的名字
                system_message = SystemMessage(
                    content=f"您是一个有用的助手，名叫 {state.get('agent_name')}..."
                )

                response = ChatOpenAI(model="gpt-5.2").invoke(
                    [system_message, *state["messages"]],
                    config
                )

                return {
                    **state,
                    "messages": response,
                }
            ```
        </Tab>
        <Tab value="TypeScript">
            ```typescript title="agent-js/src/agent.ts"
            import { interrupt } from "@langchain/langgraph"; // [!code highlight]
            import { SystemMessage } from "@langchain/core/messages";
            import { ChatOpenAI } from "@langchain/openai";

            // 添加上一步的 agent 状态定义
            export const AgentStateAnnotation = Annotation.Root({
                agentName: Annotation<string>,
                ...CopilotKitStateAnnotation.spec,
            });
            export type AgentState = typeof AgentStateAnnotation.State;

            async function chat_node(state: AgentState, config: RunnableConfig) {
                const agentName = state.agentName
                ?? interrupt("在我们开始之前，您想叫我什么名字？"); // [!code highlight]

                // 告诉 Agent 它的名字
                const systemMessage = new SystemMessage({
                    content: `您是一个有用的助手，名叫 ${agentName}...`,
                });

                const response = await new ChatOpenAI({ model: "gpt-5.2" }).invoke(
                    [systemMessage, ...state.messages],
                    config
                );

                return {
                    ...state,
                    agentName,
                    messages: response,
                };
            }
            ```
        </Tab>
    </Tabs>
</Step>
<Step>
    ### 在前端处理中断
    此时，LangGraph Agent 的 `interrupt` 将被调用。但是，我们目前还没有处理在前端渲染或
    响应中断的逻辑。

    为此，我们将使用 `useInterrupt` Hook，给它一个渲染组件，然后调用 `resolve` 并传入用户响应。

    ```tsx title="app/page.tsx"
    import { useInterrupt } from "@copilotkit/react-core/v2"; // [!code highlight]
    // ...

    const YourMainContent = () => {
    // ...
    // [!code highlight:15]
    // 为简洁起见省略样式
    useInterrupt({
        render: ({ event, resolve }) => (
            <div>
                <p>{event.value}</p>
                <form onSubmit={(e) => {
                    e.preventDefault();
                    resolve((e.target as HTMLFormElement).response.value);
                }}>
                    <input type="text" name="response" placeholder="输入您的回复" />
                    <button type="submit">提交</button>
                </form>
            </div>
        )
    });
    // ...

    return <div>{/* ... */}</div>
    }
    ```

</Step>
<Step>
### 试一试！
试着与您的 Agent 对话，您会看到它现在暂停执行并等待您回复！
<video src="https://cdn.copilotkit.ai/docs/copilotkit/images/coagents/interrupt-flow.mp4" className="rounded-lg shadow-xl" loop playsInline controls autoPlay muted />
</Step>
</Steps>

## 高级用法

### 条件 UI 执行

在 Agent 中渲染多个 `interrupt` 事件时，UI 中多个 `useInterrupt` Hook 调用之间可能会发生冲突。
因此，该 Hook 可以接受 `enabled` 参数来有条件地应用：

<Steps>
    <Step>
        ### 定义多个中断
        首先，让我们定义两个不同的中断。我们将包含一个"type"属性来区分它们。
        <Tabs groupId="language_langgraph_agent" items={['Python', 'TypeScript']} default="Python" persist>
            <Tab value="Python">
                ```python title="agent.py"
                from langgraph.types import interrupt # [!code highlight]
                from langchain_core.messages import SystemMessage
                from langchain_openai import ChatOpenAI

                # ... 您的完整状态定义

                def chat_node(state: AgentState, config: RunnableConfig):

                  state["approval"] = interrupt({ "type": "approval", "content": "请批准" }) # [!code highlight]

                  if not state.get("agent_name"):
                    # 中断并等待用户回复名称
                    state["agent_name"] = interrupt({ "type": "ask", "content": "在我们开始之前，您想叫我什么名字？" }) # [!code highlight]

                  # 告诉 Agent 它的名字
                  system_message = SystemMessage(
                    content=f"您是一个有用的助手..."
                  )

                  response = ChatOpenAI(model="gpt-5.2").invoke(
                    [system_message, *state["messages"]],
                    config
                  )

                  return {
                    **state,
                    "messages": response,
                  }
                ```
            </Tab>
            <Tab value="TypeScript">
                ```typescript title="agent-js/src/agent.ts"
                import { interrupt } from "@langchain/langgraph"; // [!code highlight]
                import { SystemMessage } from "@langchain/core/messages";
                import { ChatOpenAI } from "@langchain/openai";

                // ... 您的完整状态定义

                async function chat_node(state: AgentState, config: RunnableConfig) {
                  state.approval = await interrupt({ type: "approval", content: "请批准" }); // [!code highlight]

                  if (!state.agentName) {
                    state.agentName = await interrupt({ type: "ask", content: "在我们开始之前，您想叫我什么名字？" }); // [!code highlight]
                  }

                  // 告诉 Agent 它的名字
                  const systemMessage = new SystemMessage({
                    content: `您是一个有用的助手...`,
                  });

                  const response = await new ChatOpenAI({ model: "gpt-5.2" }).invoke(
                    [systemMessage, ...state.messages],
                    config
                  );

                  return {
                    ...state,
                    messages: response,
                  };
                }
                ```
            </Tab>
        </Tabs>
    </Step>
    <Step>
        ### 添加多个前端处理程序
        考虑到区分器，我们将添加一个处理任何"ask"和任何"approve"类型的处理程序。
        在我们的页面中使用两个 `useInterrupt` Hook，我们可以利用 `enabled` 属性在正确的时间启用每个：

        ```tsx title="app/page.tsx"
        import { useInterrupt } from "@copilotkit/react-core/v2"; // [!code highlight]
        // ...

        const ApproveComponent = ({ content, onAnswer }: { content: string; onAnswer: (approved: boolean) => void }) => (
            // 为简洁起见省略样式
            <div>
                <h1>您批准吗？</h1>
                <button onClick={() => onAnswer(true)}>批准</button>
                <button onClick={() => onAnswer(false)}>拒绝</button>
            </div>
        )

        const AskComponent = ({ question, onAnswer }: { question: string; onAnswer: (answer: string) => void }) => (
        // 为简洁起见省略样式
            <div>
                <p>{question}</p>
                <form onSubmit={(e) => {
                    e.preventDefault();
                    onAnswer((e.target as HTMLFormElement).response.value);
                }}>
                    <input type="text" name="response" placeholder="输入您的回复" />
                    <button type="submit">提交</button>
                </form>
            </div>
        )

        const YourMainContent = () => {
            // ...
            // [!code highlight:13]
            useInterrupt({
                enabled: ({ eventValue }) => eventValue.type === 'ask',
                render: ({ event, resolve }) => (
                    <AskComponent question={event.value.content} onAnswer={answer => resolve(answer)} />
                )
            });

            useInterrupt({
                enabled: ({ eventValue }) => eventValue.type === 'approval',
                render: ({ event, resolve }) => (
                    <ApproveComponent content={event.value.content} onAnswer={answer => resolve(answer)} />
                )
            });

            // ...
        }
    ```
    </Step>
</Steps>

### 中断的预处理和程序化处理中断值

当选择自定义聊天 UI 时，某些情况可能需要对中断事件的传入值进行预处理，甚至完全解决它而不为其显示 UI。
这可以使用 `handler` 属性来实现，它不需要返回 React 组件。

处理程序的返回值将作为 `result` 参数传递给 `render` 方法。
```tsx title="app/page.tsx"
// 我们假设以下形状的中断事件
type Department = 'finance' | 'engineering' | 'admin'
interface AuthorizationInterruptEvent {
    type: 'auth',
    accessDepartment: Department,
}

import { useInterrupt } from "@copilotkit/react-core/v2";

const YourMainContent = () => {
    const [userEmail, setUserEmail] = useState({ email: 'example@user.com' })
    function getUserByEmail(email: string): { id: string; department: Department } {
        // ... 用户获取的实现
    }

    // ...
    // 为简洁起见省略样式
    // [!code highlight:28]
    useInterrupt({
        handler: async ({ result, event, resolve }) => {
            const { department } = await getUserByEmail(userEmail)
            if (event.value.accessDepartment === department || department === 'admin') {
                // 解决事件后，我们不会继续到 render 方法
                resolve({ code: 'AUTH_BY_DEPARTMENT' })
                return;
            }

            return { department, userId }
        },
        render: ({ result, event, resolve }) => (
            <div>
                <h1>{event.value.type} 请求</h1>
                <p>{result.department} 部门的成员无法访问此信息</p>
                <p>您可以向管理员请求访问权限以继续。</p>
                <button
                    onClick={() => resolve({ code: 'REQUEST_AUTH', data: { department: result.department, userId: result.userId } })}
                >
                    请求访问
                </button>
                <button
                    onClick={() => resolve({ code: 'CANCEL' })}
                >
                    取消
                </button>
            </div>
        )
    });
    // ...

    return <div>{/* ... */}</div>
}
```
